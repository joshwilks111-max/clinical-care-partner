# v3.1 Build-Ready Plan — Heidi Take-Home Thin-Harness / Fat-Skill

**Source:** `/plan-eng-review` of `C:\Users\joshw\Downloads\thin-harness-v3.1-plan.html` + patch + skill-creator prompt, with the built skill at `C:\Users\joshw\skills\dose-calculator\` and the parallel iteration-2 brief locked in this session.
**Reviewed:** 2026-05-28 against the actual SKILL.md contract (226/226 PASS across 17 cases at iteration-2-post-cleanup) and the SDK 6 `node_modules/ai@6.0.191/dist/index.d.ts` callback shape.
**Branch:** `claude/elegant-robinson-17acc6` · **Worktree:** `.claude/worktrees/elegant-robinson-17acc6` · 13 commits behind `origin/main` at review time.
**Deadline:** Monday 5pm AEST (2026-06-01) — ~3.5d buffer at planning time, ~3.5–4hr Sonnet sequential build or ~2hr Opus parallel.
**Status:** LOCKED through 20 decisions (D1–D20). Build-ready. (D13–D17 added by design-review 2026-05-28; D18–D20 added by devex-review 2026-05-28.)

---

## §0 — What this plan IS and IS NOT

**IS:** the executable build plan that ships v3.1 of the thin-harness/fat-skill rewrite, picks up the skill from `~/skills/dose-calculator/` (and its iteration-2 DEPTH additions), and lands on `claude/elegant-robinson-17acc6` ready to merge into `main` and deploy.

**IS NOT:** a re-argument of any locked decision. D1–D20 are settled; build agents execute, not re-deliberate. If a decision needs re-opening, that's a new review pass, not an inline negotiation.

---

## §1 — Locked decisions (D1–D20) — the contract every agent honours

| # | Decision | Why it matters |
|---|---|---|
| **D0** | **Scope:** v3.1 rewrite ships (NOT the wrap-up plan from yesterday's design doc). User has buffer. | Commits to rewriting, not polishing. |
| **D1** | **`streamText` for the chat prose**, server-side validator runs in `onFinish`, UI fills dose-card from validated tool result. | Brief asks for a clinical-AI feel; streaming carries that signal. |
| **D2** | **Validator walks `event.steps[].toolResults`** to find the `calculate_dose` call by `tool_call_id`. Aggregates across the multi-step loop. | Plan's `{ text, toolCalls, toolResults }` destructure is wrong for SDK 6 — confirmed against `node_modules/ai/dist/index.d.ts`. |
| **D3** | **Split refusal surface:** `load_guideline` owns `out_of_scope` + `region_unknown`; skill owns `unresolved_dangers` (direct prose abstention); `calculate_dose` keeps its existing 4-value union. | Clinically honest — "no guideline" is retrieval; "differential too wide" is judgement. |
| **D4** | **Migrate today's 2-column console to a Heidi-grammar 3-column shell.** ~~Keep Bluey 3-column console shell~~ — there is no Bluey shell and no `EvidencePanel` in today's source (`app/console/console.tsx:219` is `md:grid-cols-[300px_1fr]` with `CasePanel` left + stepped Turn 1/Turn 2 cards right). v3.1 migrates the whole console to a NEW 3-column layout (locked Variant A, design-review 2026-05-28): 220px LEFT rail (stubbed sessions list = the 5 demo cases) / 700px CENTRE note pane (replaces today's stepped Turn 1/Turn 2 stack; CasePanel's extracted-facts table moves here as a collapsed accordion under the note) / 520px RIGHT rail = new `<ChatPanel>`. Chat ≥1/3 of viewport at 1440px (user-locked). Breakpoints: `220/700/520` ≥1500px; `200/1fr/480` 1180–1500px; `180/1fr/420` 1024–1180px. Mobile <1024px deferred (TODO). Visual reference: `~/.gstack/projects/joshwilks111-max-clinical-care-partner/designs/heidi-chat-right-rail-20260528/variant-A-heidi-grammar.html`. | Matches the actual Heidi product layout (verified against user screenshot 2026-05-28); the prior "Bluey shell" framing was grounded in a UI that doesn't exist in the repo. |
| **D5** | **AU + NZ regions** (not US). Use a real, verified citation — recommend Royal Children's Hospital Melbourne 2020 or NSW Health 2018 for AU; Starship Children's Health NZ 2020 stays. | Reviewer-fit: Heidi is Melbourne-based; AU clinicians evaluate this. |
| **D6** | **Delete `lib/note-discriminator-scan.ts`** (523 LOC). Skill Phase 2 prose-only differential is sufficient for croup-only. Document the tradeoff in DESIGN.md §3. | Brief doesn't ask for defense-in-depth scanner; -523 LOC win. |
| **D7** | **Two-layer safety framing in DESIGN.md:** primary = deterministic tool boundary (`calculate_dose` is a TS function, not an SDK tool route); secondary = JSON-block validator (defense-in-depth on prose). | The structural property exists with or without the validator — DESIGN.md must name both. |
| **D8** | **Skill committed to repo at `skills/dose-calculator/`** (NOT loaded from `~/skills/`). Includes SKILL.md, README.md, `references/`, `scripts/validate_dose_card.ts`, `evals/cases.jsonl`. | Reviewer must be able to read the skill from the clone. |
| **D9** | **Iteration-2-post-cleanup DEPTH absorbed:** 4 tools (`load_guideline`, `calculate_dose`, `get_reassessment_plan`, `ask_user`), 2 cards (dose-card + reassessment-card), **17 eval cases** (12 originals + 5 adversarial: prompt-injection, age-out-of-band, weight-in-pounds, conflicting-weights, severity-asserted-vs-features), `references/` committed (workflow-shape only — clinical content moved to registry). | +~150 LOC, +~12 tests; build budget grows to ~3.5hr Sonnet / ~2hr Opus. |
| **D10** | **Schema import via tsconfig path:** harness imports Zod schemas directly from `@skills/dose-calculator/scripts/validate_dose_card`. Single source of truth. | Zero drift between skill contract and harness validator. |
| **D11** | **Worktree-per-lane, cherry-pick fan-in.** Each Phase 2 lane runs in its own git worktree on its own branch (`lane-B-registry-tools`, `lane-C-validator-lib`, `lane-D-skill-sync`, `lane-E-harness`, `lane-F-ui`). Fan-in phase cherry-picks each lane's commits into `claude/elegant-robinson-17acc6`. | Visible audit trail; conflicts surface as git merges; bisectable. |
| **D12** | **Manual launch from 5 desktop-app sessions.** You spawn 5 Claude Code sessions in the desktop app's side panel (NOT 5 terminal windows). Each session's working directory is the corresponding worktree. You paste one lane prompt per session. The MAIN Claude Code session is the operator: it runs Phase 1, executes `scripts/dx/fan-out.sh` to create all 5 worktrees in one shot (eliminates the 5x manual `<BASE_SHA>` substitution per D18), watches lane progress via per-lane `PROGRESS.md` files (D19), and runs `scripts/dx/fan-in-check.sh` to verify all 5 lanes pushed green before Phase 3. The side-panel sessions are the lanes themselves. | Most reliable; the desktop app handles window layout; main Claude can `cat` PROGRESS.md files without flipping between sessions; debug-friendly. (Updated 2026-05-28 by /plan-devex-review: was "5 terminals", clarified to desktop-app side-panel model.) |
| **D13** | **`originalNote` pin is server-derived, not client-passed.** The harness route stores the first user turn server-side (in-memory session map keyed on a `session_id` cookie set on first POST). Subsequent turns retrieve the pinned note from session storage; the client cannot send, override, or forge it. Defends against the `client-side-provenance-forgery` failure class (prior learning, cross-model 10/10 confidence, 2026-05-26). Locked design-review 2026-05-28. | Audit story survives a malicious or buggy client; the dose ground always traces to the FIRST user turn the route saw, not whatever the latest client said. ~30min added to Lane C (session map + session-id cookie helper). |
| **D14** | **UI design language is locked via Variant A.** 4 new CSS variables (`--cream` `#f6efe6`, `--cream-2` `#ede5d4`, `--claret` `#5b2230`, `--serif` font-stack with Charter/Source-Serif/Iowan/Georgia fallback). Existing tokens (`--safety`, `--accent`, `--det`, `--llm`, `--ink`, `--muted`, `--line`) survive untouched. Safety semaphore from DESIGN.md§233 honoured: amber = INTENTIONAL CLINICAL DECISION (refusal, capped, completeness, confirm-weight); red = TECHNICAL FAILURE (validator-blocked, model unreachable). Lane F must NOT introduce a new amber or red variant. | Single design-system source of truth across Lane F components; eliminates per-card style drift. |
| **D15** | **Suggested-prompt trinity** populates the empty ChatPanel: three fixed `<Button variant="outline">` chips — "What dose?" / "What should I watch for?" / "Differential?" — visible BEFORE a note is pasted, so the empty state communicates capability. Clicking a chip pre-fills the composer (does NOT auto-submit). The 5 demo cases live in the LEFT rail's stubbed session list, NOT as suggested prompts. | Empty state earns its existence; the first-impression problem is solved with three chips, not a hero. |
| **D16** | **`+ New chat` is a hard reset.** Clicking it clears: thread, composer, and server-side `originalNote` pin (via DELETE on session-id). Centre note pane is retained but ready to repaste. If the thread has ≥1 message, a confirm dialog fires first: "Start a new chat? Current conversation will be cleared." Matches Heidi's "+ New chat" semantics from the screenshot. | Audit trail stays clean: each chat is one bounded session with one pinned note; cross-chat context bleed is impossible. |
| **D17** | **Full a11y pass in v1.** ARIA landmarks (`role="complementary"` left, `role="main"` centre, `role="region"` right + `role="log" aria-live="polite"` thread), per-message `<article aria-label>`, dose-card and reassessment-card with semantic `aria-label`s describing the value/timer/branches, inline-cite pills with `aria-label="Source: <name>"`, validator-blocked alerts `aria-live="assertive"`, `:focus-visible 2px claret offset 2px` on all interactives, `prefers-reduced-motion` honoured on shimmer/thinking-dots, contrast AA verified (white-on-claret 11.4:1, muted-on-cream 4.7:1). +~1.5hr to Lane F. | Clinical-AI reviewer values a11y heavily; Heidi runs in clinical contexts where keyboard-only / screen-reader use is real. |
| **D18** | **Operator-side fan-out is scripted, not manual.** The main Claude Code session runs `scripts/dx/fan-out.sh $BASE_SHA` after Phase 1 — the script creates all 5 worktrees, checks out the 5 lane branches off `$BASE_SHA`, runs `npm install` in each, and prints a `LANES_READY` line with the 5 worktree paths to spawn side-panel sessions in. Eliminates 5x manual `<BASE_SHA>` substitution; eliminates the "lane built off the wrong commit, discovered at cherry-pick" silent-failure class. Locked /plan-devex-review 2026-05-28. | Single source of truth for the SHA; one script the main Claude can audit before running; mistake-impossible because no human typing. |
| **D19** | **Per-lane `PROGRESS.md` files for in-flight status.** Each lane prompt requires the Opus session to write `.claude/worktrees/lane-X/PROGRESS.md` every ~10 minutes with a one-line status: `[T+15m] DONE: <files+test counts> · WIP: <current file> · BLOCKERS: <none\|description>`. The main Claude session checks all 5 PROGRESS.md files via a single `cat` in one Bash call — no need to flip into each side-panel session. Eliminates the "lane silent for an hour, then I notice" failure class. Locked /plan-devex-review 2026-05-28. | Glanceable progress without context-switch into each lane; surfaces stuck lanes in <10 min instead of >30. |
| **D20** | **Bounded fix-loop per lane.** Each lane prompt includes a `## Failure mode` block: "If your sign-off tests fail, you have ONE attempt to triage + fix. If a second test run still fails, STOP. Write the failure summary to PROGRESS.md with `STATUS: BLOCKED` and the specific error. Do not loop. The main session will triage." Bounds the failure path without prescribing tactics; prevents the "Lane F loops for 30 min on a tsc error" class. Locked /plan-devex-review 2026-05-28. | Caps wasted operator-Claude wall time at ~2 lane-iterations; surfaces blockers fast. |

---

## §2 — Architecture — three layers, one boundary

```
┌────────────────────────────────────────────────────────────────────────────────┐
│  FAT SKILL — skills/dose-calculator/ (committed, iteration-2)                  │
│                                                                                │
│  SKILL.md ........... 5 phases (Triage → Diagnose → Retrieve → Calculate →     │
│                       Reassess), 5 invariants, 2 JSON-block templates           │
│  README.md .......... portability + DCB0129 hazard log + TGA carve-out          │
│  references/                                                                    │
│    ├─ croup-flowchart.md ......... workflow shape (state machine; clinical      │
│    │                                content lives in the registry)              │
│    └─ refusal-taxonomy.md ........ each RefusalKind + DCB0129 gloss             │
│  scripts/                                                                       │
│    └─ validate_dose_card.ts ...... Zod schemas (DoseCardEmittedSchema +         │
│                                     ReassessmentCardEmittedSchema)              │
│  evals/                                                                         │
│    └─ cases.jsonl ................ 17 cases (12 + 5 adversarial)                │
└────────────────────────────────────────────────────────────────────────────────┘
                          │ system prompt (loaded by harness)
                          │ Zod schemas (tsconfig-path import)
                          ▼
┌────────────────────────────────────────────────────────────────────────────────┐
│  THIN HARNESS — app/api/chat/route.ts + lib/* (~250 LOC)                       │
│                                                                                │
│  · streamText (Vercel AI SDK 6.0.191, claude-opus-4-7)                         │
│  · System prompt = SKILL.md (hot-reload in dev, baked at build in prod)        │
│  · Tools registered: load_guideline, calculate_dose, get_reassessment_plan,    │
│                       ask_user                                                  │
│  · Sticky context: originalNote (set on first user turn) + trailing chat       │
│  · onFinish → lib/response-validator.ts                                        │
│  · Returns { text, dose_card | null, reassessment_card | null, refusal | null }│
└────────────────────────────────────────────────────────────────────────────────┘
                          │
                          ▼
┌────────────────────────────────────────────────────────────────────────────────┐
│  FAT CODE — deterministic tools + registry                                     │
│                                                                                │
│  · tools/calculate_dose.ts ........ UNCHANGED from origin/main (296 LOC,       │
│                                      30 tests). The deterministic spine.       │
│  · tools/load_guideline.ts ........ NEW. Wraps registry; typed refusals.       │
│  · tools/get_reassessment_plan.ts . NEW. Wraps registry; typed refusals.       │
│  · tools/ask_user.ts .............. NEW. Structured slot tool.                 │
│  · tools/types.ts ................. NEW. Re-exports skill Zod schemas;         │
│                                      defines RefusalKind unions.                │
│  · lib/response-validator.ts ...... NEW. Walks event.steps[], validates both   │
│                                      cards, merges tool results.                │
│  · lib/skill-loader.ts ............ NEW. Dev/prod system-prompt loader.        │
│  · lib/region.ts .................. NEW. Cookie helper, default NZ, AU         │
│                                      explicit.                                  │
│  · lib/tool-call-id.ts ............ NEW. nanoid generator,                     │
│                                      ^[a-zA-Z0-9_-]{8,32}$.                     │
│  · registry/guidelines.ts ......... EXTEND. Add region field + AU guideline +  │
│                                      reassessment_plans array. Remove           │
│                                      anaphylaxis (deferred).                    │
└────────────────────────────────────────────────────────────────────────────────┘
                          │
                          ▼
┌────────────────────────────────────────────────────────────────────────────────┐
│  UI — Heidi-grammar 3-column console (FULL MIGRATION, not a rail-swap)         │
│                                                                                │
│  Grid: 220px / 700px / 520px  (≥1500px)                                        │
│         200px /  1fr  / 480px (1180–1500px)                                    │
│         180px /  1fr  / 420px (1024–1180px) — left rail icons-only             │
│         mobile <1024px DEFERRED (TODO)                                         │
│                                                                                │
│  LEFT RAIL (220px) ─ stubbed sessions list                                     │
│    ├─ <H> logo (claret #5b2230, serif)                                         │
│    ├─ <Button> "+ New session" (claret primary)                                │
│    ├─ Nav: Scribe / Evidence / Tasks  (Lucide icons; active = cream-2 bg)      │
│    └─ Sessions, grouped "Today" / "Yesterday":                                 │
│         • Jack T · croup (NZ)        ← demo case 1 (today's demo-buttons       │
│         • Jack T · croup (AU)        ← demo case 2     pattern, re-homed       │
│         • Mia R · ?epiglottitis      ← demo case 3     here)                   │
│         • Weightless transcript      ← demo case 4                             │
│         • Asthma 5yo (out_of_scope)  ← demo case 5                             │
│       Clicking a session loads its note into the CENTRE column.                │
│                                                                                │
│  CENTRE (700px) ─ the case (Note pane; replaces today's stepped Turn1/Turn2)   │
│    ├─ Case header: avatar + name + age + weight                                │
│    ├─ Meta strip: date · timer · "Region: NZ"                                  │
│    ├─ Action bar: <Button "Create plan"> (claret) <Button "Resume"> (ghost)    │
│    ├─ Tabs: Note (active) / Transcript / Templates                             │
│    ├─ <Textarea> (paste a note OR rendered note) — utilitarian, "big enough    │
│    │   to paste into"; not a hero region                                       │
│    └─ Collapsed accordion: Extracted facts (condition / severity / age / kg)   │
│       — survives from today's CasePanel but as secondary info                  │
│                                                                                │
│  RIGHT RAIL (520px) ─ <ChatPanel> = the Care Partner conversation              │
│    ├─ Header                                                                   │
│    │    ├─ Chip "Care Partner"                                                 │
│    │    └─ Link "+ New chat" (claret, 12.5px)                                  │
│    ├─ <Thread> ─ role="log" aria-live="polite"                                 │
│    │   EMPTY STATE: "Paste a note in the centre, then ask Care Partner."       │
│    │     + suggested-prompt trinity: [What dose?] [What should I watch for?]   │
│    │       [Differential?]  (3 <Button variant="outline"> via ButtonGroup)     │
│    │   ┌─ <Message role="user">  ── claret fill, white text, 13/13/4/13 radius │
│    │   └─ <Message role="assistant">  ── ONE STRUCTURED DOCUMENT per turn:    │
│    │        ├─ <Reasoning>  ── "Thought for Ns" chip (cream-2 bg, claret spark)│
│    │        ├─ <Sources>   ── "3 sources · View" chip (claret n, muted vw)    │
│    │        ├─ Title (serif 18–19px) ── one-line clinical headline             │
│    │        ├─ Body prose with <InlineCitation> pills (§ + source name)        │
│    │        ├─ <DoseCard>  ── EMBEDDED INSIDE the bubble, NOT a sibling.       │
│    │        │     • dose value at 22–24px serif = the VISUAL HEADLINE         │
│    │        │     • "14.2 kg × 0.15 mg/kg = 2.13 mg" trace in mono             │
│    │        │     • <Badge "DETERMINISTIC TOOL"> micro-pill                    │
│    │        │     • Source-row → InlineCitation                                │
│    │        │     • capped:true → amber "CAPPED" chip + binding-limit line     │
│    │        ├─ Section header "Reassess in N hours" (serif 14.5px)             │
│    │        ├─ Reassessment narrative (prose, one line)                        │
│    │        └─ <ReassessmentCard>  ── EMBEDDED INSIDE the bubble:              │
│    │             • clock badge + "Reassess at HH:MM (in Nh)"                   │
│    │             • watch-for chips (amber Badge × N, from tool result)         │
│    │             • 2 branch <Button variant="outline">                         │
│    │               (If worse → escalate · If improving → continue obs)         │
│    │             • universal_rails footer (dashed top border, muted)           │
│    │   Variants (per <Message role="assistant">):                              │
│    │     · Skill direct refusal → <RefusalCard> (Alert variant="safety";       │
│    │         amber-bordered; RefusalKind verbatim as mono header)              │
│    │     · Tool refusal (out_of_scope / rule_not_verified / weight_missing)    │
│    │         → same <RefusalCard> shape                                        │
│    │     · ask_user → inline <Form> inside the assistant bubble (amber-bord)   │
│    │         on submit, answer becomes the NEXT user message in the thread     │
│    │     · Validator-blocked → <Alert variant="destructive"> (red) with the    │
│    │         block reason verbatim + retry CTA                                 │
│    ├─ <Composer>                                                               │
│    │    ├─ Context chip (cream-2 bg, claret avatar): "Jack T, 3yo, 14.2 kg"    │
│    │    │   ← visualisation of the originalNote pin (server-derived, D13)      │
│    │    ├─ <PromptInput> textarea + send button (claret fill, ↑ arrow)         │
│    │    └─ Tool row: Lucide icons (Plus / FileText / BookOpen / Mic) — NOT     │
│    │       emoji. 32×32px tappable; 44px on touch viewports                    │
│    └─ Footer                                                                   │
│         ├─ One-line legalese: "Medical knowledge support. Confirm with         │
│         │  clinical judgement." (11px muted)                                   │
│         └─ <Select> "NZ ▾" region toggle (defaults NZ; switching prompts a     │
│            confirm: "Switch region? This clears the current chat.")            │
└────────────────────────────────────────────────────────────────────────────────┘

Visual reference (locked Variant A, design-review 2026-05-28):
  ~/.gstack/projects/joshwilks111-max-clinical-care-partner/designs/
    heidi-chat-right-rail-20260528/variant-A-heidi-grammar.html
  (alongside: heidi-screenshot-spec.md · approved.json · variant-B,C for context)
```

---

## §3 — File-level deltas

### New files (24)

| Path | LOC est. | Purpose |
|---|---|---|
| `skills/dose-calculator/SKILL.md` | ~320 | The fat skill (iteration-2 copy) — 5 phases, 5 invariants, 2 card templates |
| `skills/dose-calculator/README.md` | ~150 | Portability + DCB0129 hazard log + TGA carve-out |
| `skills/dose-calculator/references/croup-flowchart.md` | ~40 | Workflow shape (clinical content in registry) |
| `skills/dose-calculator/references/refusal-taxonomy.md` | ~40 | Each RefusalKind + DCB0129 gloss |
| `skills/dose-calculator/scripts/validate_dose_card.ts` | ~210 | Zod source-of-truth — exports `DoseCardEmittedSchema`, `ReassessmentCardEmittedSchema`, `CalculateDoseRefusalKind`, `ReassessmentRefusalKind`, `AnyRefusalKind`; harness imports these directly |
| `skills/dose-calculator/evals/cases.jsonl` | 17 lines | 17 eval cases |
| `app/api/chat/route.ts` | ~240 | Thin harness — streamText + 4 tools + onFinish + server-derived `originalNote` pin per D13 (session-id cookie + in-memory map) |
| `lib/response-validator.ts` | ~150 | Walks steps[]; validates dose-card + reassessment-card |
| `lib/skill-loader.ts` | ~40 | Dev: fs.readFile; prod: inline string |
| `lib/region.ts` | ~50 | getRegion(cookies); default NZ |
| `lib/tool-call-id.ts` | ~20 | nanoid generator (regex-conformant) |
| `lib/session-store.ts` | ~60 | NEW (D13) — in-memory `Map<sessionId, { originalNote, createdAt }>`; sessionId from `care-partner-session` cookie; `getOrCreatePin(req, firstUserTurn)` returns the pinned note; `clearPin(sessionId)` for `+ New chat` |
| `tools/types.ts` | ~80 | Re-exports skill Zod schemas; RefusalKind unions; LoadGuidelineRefusalKind |
| `tools/load_guideline.ts` | ~120 | Region-aware lookup; typed refusal returns; now also returns `differential_check` + per-row `severity_rows[].description` per upstream skill-cleanup (2026-05-28) |
| `tools/get_reassessment_plan.ts` | ~120 | Selection logic per spec; typed refusal returns |
| `tools/ask_user.ts` | ~40 | Structured slot tool |
| `app/console/chat-panel.tsx` | ~220 | Right-rail chat (D4/D14-D17). Empty-state suggested-prompt trinity (D15). "+ New chat" hard reset (D16). Context chip on composer shows server-derived pin (D13). |
| `app/console/dose-card.tsx` | ~100 | Card embedded INSIDE assistant bubble (D14). Dose value 22-24px serif = visual headline. capped → amber CAPPED chip + binding-limit. |
| `app/console/reassessment-card.tsx` | ~120 | Card embedded INSIDE assistant bubble. Clock badge + watch chips + 2 branch buttons + universal_rails footer. `no_reassessment_required` → single muted line, NOT a card. |
| `app/console/refusal-card.tsx` | ~80 | `<Alert variant="safety">` — amber, RefusalKind verbatim in mono small-caps. NEVER red (red is for technical failure per D14). |
| `app/console/region-toggle.tsx` | ~50 | Footer `<Select>` — NZ/AU; switch fires confirm + DELETE session-id |
| `app/console/ask-user-form.tsx` | ~80 | NEW (Pass 2 lock) — ask_user prompt as inline `<Alert variant="safety">` form inside the assistant bubble; on submit, answer becomes the NEXT user message (audit trail reads naturally). |
| `app/console/session-rail.tsx` | ~120 | NEW (D4) — 220px left rail: brand, "+ New session", nav, stubbed sessions list (5 demo cases per D15). Clicking a session loads its note into the centre. |
| `app/console/note-pane.tsx` | ~150 | NEW (D4) — 700px centre: case header + meta + action bar + tabs + textarea/rendered note + collapsed extracted-facts accordion (today's CasePanel facts table demoted to secondary). |

**Total new LOC: ~2,720** (was ~2,030; +D13 session-store +D17 a11y in Lane F components +D4 session-rail/note-pane/ask-user-form).

### Modified files (6)

| Path | Change |
|---|---|
| `app/console/console.tsx` | REPLACE entirely. Today's 2-column `md:grid-cols-[300px_1fr]` (CasePanel + stepped Turn1/2) becomes the 3-column shell per D4: `<SessionRail>` / `<NotePane>` / `<ChatPanel>`. State: `currentSession`, no `originalNote` (server-derived per D13). |
| `app/globals.css` | Add 4 design tokens per D14 (`--cream`, `--cream-2`, `--claret`, `--claret-ink`) and `--serif` font-stack. Load Inter + Source Serif 4 via next/font/google in `app/layout.tsx`. Body background → `--cream`. |
| `app/layout.tsx` | Load `Inter` + `Source_Serif_4` from `next/font/google`; apply Inter to `<html>` className; expose Source Serif 4 as `--serif` CSS variable. |
| `registry/guidelines.ts` | Add `region: "NZ"\|"AU"` field; add AU croup guideline; add `reassessment_plans[]` field per guideline; remove anaphylaxis (move to TODOS); preserve existing Starship croup data verbatim. ALSO (per 2026-05-28 skill-cleanup) add `differential_check: DifferentialItem[]` and per-row `severity_rows[].description` so the load_guideline tool can return them. |
| `tools/calculate_dose.ts` | NO CHANGES (math/refusals stay 4-valued per D3) |
| `tsconfig.json` | Add `"@skills/*": ["./skills/*"]` to paths |
| `DESIGN.md` | Rewrite per §6 below |
| `README.md` | NO REWRITE in v3.1 scope. Lane E-prep does not touch it. The existing "Try in 60 seconds" + demo-button table is strong; a separate end-of-build doc-update pass will refresh endpoint references (`/api/turn1` → `/api/chat`) and add a one-line skill-workspace pointer. (Updated 2026-05-28 by /plan-devex-review.) |
| `docs/architecture.md` + `.png` | Rewrite to 3-layer story |
| `app/console/console.test.tsx` | Update to assert 3-column shell renders (was checking Turn1/Turn2 wiring). |

### Deleted files (~−1,800 LOC net)

| Path | Lines | Why |
|---|---|---|
| `app/api/turn1/route.ts` | 386 | Pipeline replaced by /api/chat |
| `app/api/turn1.5/route.ts` | 460 | Collapse logic absorbed into skill Phase 2 |
| `app/api/turn2/route.ts` | 552 | Replaced by streamText + tools |
| `prompts/turn1.ts` | 248 | Folded into SKILL.md |
| `prompts/turn1.5.ts` | 405 | Folded into SKILL.md |
| `prompts/turn2.ts` | 256 | Folded into SKILL.md |
| `lib/collapse.ts` | 506 | Skill prose handles differential collapse |
| `lib/note-discriminator-scan.ts` | 523 | Per D6 |
| `lib/case-state.ts` | 180 | Replaced by chat session state |
| `lib/completeness.ts` | 83 | Replaced by ask_user + tool refusals |
| `lib/refusal-gate.ts` | 114 | Refusals are typed-tool-returns now |
| `lib/plan-schema.ts` | 235 | Slimmed to schemas in `tools/types.ts` |
| `app/console/case-panel.tsx` + `case-panel.test.tsx` | ~110 + ~60 | Facts table migrates into `note-pane.tsx` collapsed accordion; confirm-weight migrates into `ask-user-form.tsx` (an inline form pattern, not a sticky button). The CasePanel role doesn't survive D4. |
| `app/console/turn1-view.tsx` + `turn1-view.test.tsx` | ~120 + ~80 | Pipeline UI replaced by chat-panel + dose-card |
| `app/console/turn2-view.tsx` + `turn2-view.test.tsx` | ~150 + ~80 | Pipeline UI replaced by chat-panel + dose-card + reassessment-card |
| `app/console/phase-loader.tsx` | ~80 | Streaming phase labels replaced by Shimmer + Reasoning ("Thought for Ns") chip in chat-panel |
| `app/console/provenance-badge.tsx` | ~40 | Inline-cite pills + `DETERMINISTIC TOOL` Badge on dose-card replace the standalone provenance badge |
| `app/console/fixtures.ts` + `fixtures-weight-gate.test.ts` | ~140 + ~80 | Demo notes re-homed in `session-rail.tsx` as stubbed session entries (D15) |
| All matching `.test.ts(x)` files for the above | ~600 | Deleted with source |
| `scripts/draft-turn15-trace.ts` + `measure-prompt-tokens.ts` | ~120 | Reference deleted prompts |

**Net LOC delta: ~+2,030 new − ~4,000 deleted ≈ −1,970 LOC.**

---

## §4 — Test plan

**Framework:** vitest only. Promptfoo removed (decision 2026-05-28 — single eval surface; the skill's `evals/cases.jsonl` is consumed by `skills/dose-calculator/contract.test.ts` running under vitest, and the LLM integration test in `app/api/chat/route.test.ts` covers the end-to-end with mocked LLM responses). Remove `promptfoo` from `package.json` `devDependencies` and the `npm run eval` script during Phase 3 cleanup.

### New tests (~57)

| File | Count | Asserts |
|---|---|---|
| `lib/response-validator.test.ts` | ~14 | No fence → null card. Valid dose-card fence + matching tool call → card filled from tool result. Valid reassessment-card fence + matching tool call → filled. Malformed JSON → blocked. Schema mismatch → blocked. Orphan tool_call_id → blocked. Card on refusal → blocked. Multiple fences in one message (both cards) → both render. Walk steps[] correctly. Tool result mutation doesn't leak prose number. Whitespace tolerance. Fence inside another code block → handled. |
| `lib/skill-loader.test.ts` | ~3 | Dev fs.readFile; prod baked string; missing file errors clearly |
| `lib/region.test.ts` | ~3 | NZ default; AU explicit; malformed cookie → NZ |
| `lib/tool-call-id.test.ts` | ~3 | Conforms to ^[a-zA-Z0-9_-]{8,32}$; uniqueness across 10k calls; length stable |
| `tools/load_guideline.test.ts` | ~6 | NZ croup ok; AU croup ok; asthma → out_of_scope refusal; unknown region → fallback NZ; severity_rows shape; dose_rules shape |
| `tools/get_reassessment_plan.test.ts` | ~7 | Mild/moderate plan returns 120min; severe returns 240min; invalid_severity_label; invalid_guideline_id; rule_not_verified; no_reassessment_required for one-shot drug; branches array shape |
| `tools/ask_user.test.ts` | ~3 | kind enum; return shape; structured slot emission |
| `tools/types.test.ts` | ~4 | DoseCardEmittedSchema round-trip; ReassessmentCardEmittedSchema round-trip; RefusalKind exhaustiveness; LoadGuidelineRefusalKind exhaustiveness |
| `app/api/chat/route.test.ts` | ~7 | Jack T. NZ happy path (dose-card + reassessment-card); Jack T. AU happy path (different dose, different reassessment minutes); missing weight → ask_user → user reply → calc; unresolved_dangers refusal (skill-direct, no calculate_dose call); out_of_scope refusal (load_guideline-returned); multi-turn context retention (originalNote pinning); region toggle changes dose |
| `app/console/chat-panel.test.tsx` | ~5 | Renders messages list; fires onSubmit; shows shimmer during stream; renders DoseCard + ReassessmentCard when present; renders RefusalCard on refusal |
| `app/console/region-toggle.test.tsx` | ~2 | Cookie write/read; default NZ |
| `skills/dose-calculator/contract.test.ts` | ~17 | One per case in `cases.jsonl` (12 originals + 5 adversarial) — schema accepts each `expected_output_shape.dose_card_fields` shape; for case-11 also validates reassessment-card shape; canary for skill↔harness drift |
| `registry/guidelines.regression.test.ts` | ~3 | Existing starship-croup-2020 Jack-T-14.2-kg → 2.13 mg still produces 2.13 after schema extension. AU guideline shape round-trip. anaphylaxis removed cleanly. |

### LLM eval (vitest contract + integration tests)

- The skill's `evals/cases.jsonl` is the single source of eval truth. The contract test (`skills/dose-calculator/contract.test.ts`) reads it and asserts schema compatibility statically (no LLM call).
- The integration test (`app/api/chat/route.test.ts`) runs each scenario end-to-end against the harness with mocked tool returns per `cases.jsonl.mock_tool_returns` and the LLM either stubbed (CI) or live (`ANTHROPIC_API_KEY` set; one-off local).
- Assertions are written once, as TypeScript functions in `app/api/chat/route.test.ts`: no `\d+\s*(mg|ml|mcg|kg)` token in prose region (strip dose-card fences first); dose-card fence parses to valid Zod; reassessment-card present when expected; refusal kind verbatim when expected.
- Mechanical safety: the harness's `lib/response-validator.ts` is the runtime equivalent of the skill's `scripts/lint_skill_output.ts` + `scripts/validate_dose_card.ts` — same Zod source-of-truth, same regex guard logic.

### Surviving from main (~30)

- `tools/calculate_dose.test.ts` (30 tests) — unchanged, math regression coverage
- `lib/retry.test.ts` (10 tests) — kept for tools that need retry
- `lib/schemas.test.ts` (11 tests) — kept if any skill-side schemas reuse it
- Surviving registry tests (~10 of the original 18 — drop the anaphylaxis cases)

**Final test count: ~57 new + ~30 surviving ≈ 87 total** (down from 222 on main; safety-critical layer better-covered than today because the validator + contract test are NEW).

### Coverage diagram

```
CODE PATHS                                          USER FLOWS (brief-aligned)
─────────────                                       ──────────────────────────
[+] app/api/chat/route.ts                            [+] Paste Jack T. NZ → Send
  ├── streamText                                       └── [★★★] stream + dose-card + reassessment-card
  ├── tools (4)                              [→E2E]
  └── onFinish → validator                              [+] Paste Jack T. AU → Send
                                                        └── [★★★] different dose (RCH 2020), different
[+] lib/response-validator.ts                                 reassessment window
  ├── parse fence(s)
  ├── Zod safeParse                                   [+] Missing weight
  ├── walk event.steps[]                                └── [★★★] ask_user fires; user replies; dose calc
  ├── merge tool result                                     completes (multi-turn pinning verified)
  └── return discriminated union
                                                       [+] Unresolved dangers (epiglottitis)
[+] tools/load_guideline.ts                             └── [★★★] skill abstains in prose; refusal card;
  ├── lookup                                                no dose-card emitted
  ├── region resolution
  └── typed refusal                                    [+] Out of scope (asthma)
                                                        └── [★★★] load_guideline returns typed refusal;
[+] tools/get_reassessment_plan.ts                          skill surfaces verbatim
  ├── 6-step selection logic
  └── 6 typed refusals                                 [+] User reloads
                                                        └── [★] session resets cleanly (no persistence MVP)
[+] tools/ask_user.ts
  └── structured slot                                  [+] Region toggle mid-session
                                                        └── [★★] dose updates; reassessment plan updates
[+] tools/types.ts
  ├── re-export skill schemas                          [+] Cap firing (case-6)
  └── RefusalKind unions                                └── [★★★] 25kg severe → dose clamped, capped:true
                                                            surfaced in dose-card UI
[*] registry/guidelines.ts
  ├── add region field                                 [+] Rule not verified (case-9, stale guideline)
  ├── add AU croup                                      └── [★★★] both tools refuse cleanly
  ├── add reassessment_plans[]
  └── remove anaphylaxis                               [+] Mid-flow weight update (case-12)
                                                        └── [★★] skill re-routes; dose updates; AI Care
[*] tools/calculate_dose.ts                                   Partner shape demonstrated
  └── UNCHANGED

[+] skills/dose-calculator/* (committed)
  └── all reviewer-readable

LLM integration: [→EVAL] one vitest integration case per scenario, mocked tools

COVERAGE TARGET: 100% new paths · all user flows · 1 vitest integration case per flow · 17 eval cases (12 + 5 adversarial)
```

---

## §5 — Multi-session orchestration (5 parallel Opus terminals)

**Mode:** Phase 1 = single-session foundation (sequential, ~20min). Phase 2 = **5 parallel Opus sessions in 5 git worktrees** (~60min wall). Phase 3 = single-session fan-in + integration + ship (~60min). Total wall time **~2.5hr** (vs ~4hr sequential).

Each Phase 2 session runs in its own worktree on its own branch, pushes commits independently, and signs off when its lane's tests are green. You (the human) orchestrate the fan-in: cherry-pick each lane's commits into trunk, resolve any merge conflicts, then run Phase 3.

```
═════════════════════════════════════════════════════════════════════════════
PHASE 1 — FOUNDATION (single Opus session, you run this first, ~20min)
─────────────────────────────────────────────────────────────────────────────
Branch: claude/elegant-robinson-17acc6 (the trunk)
Working tree: .claude/worktrees/elegant-robinson-17acc6

  P1.1: git fetch origin main; rebase trunk off origin/main
        Resolve any conflicts. Drop noise checkpoints; keep real commits.
  P1.2: npm install (regenerate node_modules — confirmed ai@6.0.191 installed)
  P1.3: Update tsconfig.json with "@skills/*": ["./skills/*"] path
  P1.4: Copy ~/skills/dose-calculator/ → skills/dose-calculator/ SELECTIVELY.
        NOT `cp -r` of the whole dir (would drag node_modules + bun.lock +
        package.json into the repo — those belong to the skill's standalone
        dev workspace at ~/skills/dose-calculator/, not the harness repo).
        See PROMPT 1 step 4 in §12 for the exact 7-command copy sequence +
        post-copy assertions. Files that DO get copied:
          SKILL.md, README.md, HARNESS-BRIEF-get_reassessment_plan.md,
          references/*.md (2 files: croup-flowchart.md + refusal-taxonomy.md),
          scripts/*.ts (2 files: validate_dose_card.ts + lint_skill_output.ts),
          evals/*.jsonl (1 file: cases.jsonl, 17 lines).
        Files that do NOT get copied: node_modules/, bun.lock, package.json.
        Post-copy assertions verify D9 mechanical promises (17 cases,
        zero clinical numbers in SKILL.md or references).
  P1.5: Author tools/types.ts — re-exports skill Zod schemas + RefusalKind unions
        THIS IS THE TYPE LOCK. Phase 2 lanes import from this file.
  P1.6: Create scripts/dx/fan-out.sh + scripts/dx/fan-in-check.sh per §14
        (devex-review 2026-05-28, D18+D20). chmod +x both.
  P1.7: git add . && git commit -m "feat(v3.1 phase 1): foundation — tsconfig + skill + type lock + orchestration scripts"
  P1.8: git push origin claude/elegant-robinson-17acc6
        ★ This commit SHA is the fan-out base for Phase 2 worktrees ★
  P1.9: Capture the base SHA: BASE_SHA=$(git rev-parse HEAD)
        Then run: bash scripts/dx/fan-out.sh "$BASE_SHA"
        → creates all 5 lane worktrees + branches + npm installs.
        Echo the LANES_READY paths so you can spawn 5 desktop-app side-panel sessions.

═════════════════════════════════════════════════════════════════════════════
↓ Stop. Spawn 5 Claude Code sessions in the desktop app side panel,         ↓
↓ one per worktree path printed by fan-out.sh. Paste 5 lane prompts (§12).  ↓
═════════════════════════════════════════════════════════════════════════════

PHASE 2 — FAN-OUT (5 parallel Opus sessions, ~60min wall time)
─────────────────────────────────────────────────────────────────────────────
Each lane creates its own worktree off BASE_SHA. Branches are named lane-{X}-*.

  TERMINAL 1 — Lane B (registry + tools, ~45min)
    Worktree: .claude/worktrees/lane-B-registry-tools
    Branch:   lane-B-registry-tools (off BASE_SHA)
    Owns files (exclusive):
      registry/guidelines.ts                     [MODIFY: extend + prune]
      registry/guidelines.regression.test.ts     [NEW]
      tools/load_guideline.ts                    [NEW ~100 LOC]
      tools/load_guideline.test.ts               [NEW ~6 tests]
      tools/get_reassessment_plan.ts             [NEW ~120 LOC, per spec]
      tools/get_reassessment_plan.test.ts        [NEW ~7 tests]
      tools/ask_user.ts                          [NEW ~40 LOC]
      tools/ask_user.test.ts                     [NEW ~3 tests]
    Sign-off: npm test -- registry tools && git push origin lane-B-registry-tools

  TERMINAL 2 — Lane C (validator + lib, ~45min)
    Worktree: .claude/worktrees/lane-C-validator-lib
    Branch:   lane-C-validator-lib (off BASE_SHA)
    Owns files (exclusive):
      lib/response-validator.ts                  [NEW ~150 LOC]
      lib/response-validator.test.ts             [NEW ~14 tests]
      lib/skill-loader.ts                        [NEW ~40 LOC]
      lib/skill-loader.test.ts                   [NEW ~3 tests]
      lib/region.ts                              [NEW ~50 LOC]
      lib/region.test.ts                         [NEW ~3 tests]
      lib/tool-call-id.ts                        [NEW ~20 LOC]
      lib/tool-call-id.test.ts                   [NEW ~3 tests]
    Sign-off: npm test -- lib && git push origin lane-C-validator-lib

  TERMINAL 3 — Lane D (skill workspace sync + contract test, ~30min)
    Worktree: .claude/worktrees/lane-D-skill-sync
    Branch:   lane-D-skill-sync (off BASE_SHA)
    Owns files (exclusive):
      skills/dose-calculator/contract.test.ts    [NEW ~12 tests]
      skills/dose-calculator/SKILL.md            [REFRESH from ~/skills if updated]
      skills/dose-calculator/README.md           [REFRESH]
      skills/dose-calculator/references/*.md     [REFRESH]
      skills/dose-calculator/scripts/*.ts        [REFRESH]
      skills/dose-calculator/evals/cases.jsonl   [REFRESH]
    Sign-off: npm test -- skills/dose-calculator && git push origin lane-D-skill-sync
    Note: Lane D may finish first (it's mostly file-copy + one test file).

  TERMINAL 4 — Lane F (UI components, ~60min)
    Worktree: .claude/worktrees/lane-F-ui
    Branch:   lane-F-ui (off BASE_SHA)
    Owns files (exclusive — all NEW):
      app/console/chat-panel.tsx                 [NEW ~180 LOC]
      app/console/dose-card.tsx                  [NEW ~80 LOC]
      app/console/reassessment-card.tsx          [NEW ~100 LOC]
      app/console/refusal-card.tsx               [NEW ~60 LOC]
      app/console/region-toggle.tsx              [NEW ~50 LOC]
      app/console/chat-panel.test.tsx            [NEW ~5 tests]
      app/console/region-toggle.test.tsx         [NEW ~2 tests]
      app/console/dose-card.test.tsx             [NEW ~3 tests]
      app/console/reassessment-card.test.tsx     [NEW ~3 tests]
      app/console/refusal-card.test.tsx          [NEW ~3 tests]
    DOES NOT TOUCH: app/console/console.tsx (Lane G modifies that)
    Sign-off: npm test -- app/console && git push origin lane-F-ui

  TERMINAL 5 — Lane E-prep (DESIGN.md + docs only, ~30min)
    Worktree: .claude/worktrees/lane-E-prep
    Branch:   lane-E-prep (off BASE_SHA)
    Owns files (exclusive — all REWRITE/NEW):
      DESIGN.md                                  [REWRITE per §6 outline]
      docs/architecture.md                       [REWRITE 3-layer story]
      docs/iteration-trace/v3.1.md               [NEW ~40 LOC]
    Note: Promptfoo removed (2026-05-28 — single eval surface). Lane E-prep
          is now doc-only; the LLM eval lives in app/api/chat/route.test.ts
          (Phase 3, vitest with mocked tools).
    Sign-off: npx markdownlint DESIGN.md docs/architecture.md && git push origin lane-E-prep

═════════════════════════════════════════════════════════════════════════════
↓ Wait for all 5 lanes to push. Sign-off check below. ↓
═════════════════════════════════════════════════════════════════════════════

FAN-IN CHECK (you, ~5min)
─────────────────────────
After all 5 lanes push, run from trunk worktree:

  for lane in B-registry-tools C-validator-lib D-skill-sync F-ui E-prep; do
    git log lane-$lane --oneline | head -5
    echo "---"
  done

Expected: each lane shows 1-3 commits with WIP: or feat: prefix, all
green-tested locally per the sign-off commands.

═════════════════════════════════════════════════════════════════════════════

PHASE 3 — FAN-IN + HARNESS + SHIP (single Opus session, ~60min)
─────────────────────────────────────────────────────────────────────────────
Back to trunk worktree: .claude/worktrees/elegant-robinson-17acc6
Branch: claude/elegant-robinson-17acc6

  P3.1: Cherry-pick each lane in dependency order:
        git cherry-pick BASE_SHA..lane-D-skill-sync       # contract.test.ts first
        git cherry-pick BASE_SHA..lane-B-registry-tools   # tools the harness imports
        git cherry-pick BASE_SHA..lane-C-validator-lib    # validator the harness imports
        git cherry-pick BASE_SHA..lane-F-ui               # UI components
        git cherry-pick BASE_SHA..lane-E-prep             # evals + DESIGN.md
        Resolve conflicts if any (file isolation per §5 should make this
        rare — only if two lanes both modified e.g. tsconfig.json).

  P3.2: npm install                                       # in case package.json moved
  P3.3: npm test                                          # all lane tests still green together?
        If red: triage — typically a cross-lane type mismatch
        the type-lock didn't catch. Fix in trunk, commit.

  P3.4: BUILD THE HARNESS (this is the work that needs all lanes joined):
        app/api/chat/route.ts                  [NEW ~220 LOC]
          streamText + 4 tools (load_guideline, calculate_dose,
          get_reassessment_plan, ask_user)
          System prompt = lib/skill-loader.ts
          onFinish → lib/response-validator.ts
          Pin originalNote on first user turn
          tool_call_id from lib/tool-call-id.ts
        app/api/chat/route.test.ts             [NEW ~7 integration tests]
        npm test -- app/api/chat                 # green before continuing

  P3.5: WIRE THE UI:
        app/console/console.tsx                [MODIFY: swap EvidencePanel
                                                for ChatPanel; thread originalNote]
        app/console/bluey-shell.regression.test.tsx [UPDATE assertion]
        npm test                                 # full suite green

  P3.6: MANUAL SMOKE:
        npm run dev
        Paste Jack T. NZ note → verify stream + dose-card + reassessment-card
        Paste Jack T. AU note → verify different dose, different reassessment
        Paste no-weight note  → verify ask_user → reply → dose
        Paste epiglottitis note → verify direct refusal
        Type "asthma 5yo dose?" → verify out_of_scope refusal

  P3.7: LLM INTEGRATION EVAL (vitest):
        npx vitest run app/api/chat/route.test.ts
        All 7 integration cases must pass with mocked tool returns from
        skills/dose-calculator/evals/cases.jsonl.
        (Promptfoo removed 2026-05-28 — single eval surface.)

  # Reordered 2026-05-28 by ceo-review: deploy preview BEFORE delete-phase so
  # if Vercel rejects the new chat route, the old pipeline survives on main
  # until the new code is fixed. Rollback posture > merge-cleanliness.

  P3.8: PRE-DELETE GATES (run all locally, all must pass):
        npm test                                # ~87 green, includes new chat route tests
        npx tsc --noEmit                        # typecheck clean
        npm run build                           # production build clean LOCALLY

  P3.9: PUSH + PREVIEW DEPLOY (old code still in tree):
        git push origin claude/elegant-robinson-17acc6
        → Vercel auto-deploys the branch as a preview URL
        → Wait for the deploy comment on the push (or check `vercel ls`)

  P3.10: SMOKE PREVIEW (the new chat route on the real Vercel runtime):
        - Paste Jack T NZ note → verify stream + DoseCard + ReassessmentCard
        - Paste Jack T AU note → verify different dose
        - Paste no-weight → verify ask_user inline form
        - Paste epiglottitis → verify unresolved_dangers refusal
        - Type "asthma 5yo dose?" → verify out_of_scope refusal
        If ANY of these fails on preview: STOP. Fix locally, push again, re-smoke.
        Do NOT proceed to P3.11.

  P3.11: DELETE PHASE (only after preview smoke is green):
        git rm -r app/api/turn1/ app/api/turn1.5/ app/api/turn2/
        git rm prompts/turn1.ts prompts/turn1.5.ts prompts/turn2.ts
        git rm lib/collapse.ts lib/note-discriminator-scan.ts lib/case-state.ts
        git rm lib/completeness.ts lib/refusal-gate.ts lib/plan-schema.ts
        git rm app/console/case-panel.tsx case-panel.test.tsx
        git rm app/console/turn1-view.tsx turn1-view.test.tsx
        git rm app/console/turn2-view.tsx turn2-view.test.tsx
        git rm app/console/phase-loader.tsx provenance-badge.tsx
        git rm app/console/fixtures.ts fixtures-weight-gate.test.ts
        git rm scripts/draft-turn15-trace.ts scripts/measure-prompt-tokens.ts
        git commit -m "chore(v3.1): delete pipeline routes + dead libs + obsolete console views"

  P3.12: PUSH DELETE + SECOND PREVIEW SMOKE:
        git push origin claude/elegant-robinson-17acc6
        Wait for Vercel preview to redeploy on the delete commit.
        Re-run the 5-case smoke from P3.10. The new chat surface must still
        work after the delete (catches any accidental cross-file imports).
        If preview is green: continue. If red: `git revert HEAD` and triage.

  P3.13: docs/architecture.png regenerate (mermaid → png)
  P3.14: README.md — NO REWRITE in this phase. Per /plan-devex-review 2026-05-28, the existing "Try in 60 seconds" structure is strong and will be refreshed (endpoint references only) in a separate end-of-build doc-update pass run by the operator after Phase 3 lands.
  P3.15: gh pr create --base main --title "v3.1: thin harness + fat skill"
  P3.16: Delete lane worktrees (optional cleanup):
         git worktree remove .claude/worktrees/lane-B-registry-tools
         ... etc for C, D, E-prep, F
         git branch -D lane-B-registry-tools lane-C-validator-lib \
                       lane-D-skill-sync lane-E-prep lane-F-ui

═════════════════════════════════════════════════════════════════════════════

FILE-ISOLATION MATRIX (verify before launch)
────────────────────────────────────────────
                    Lane B  Lane C  Lane D  Lane E-prep  Lane F  Phase 3
registry/            WRITE   —       —       —            —       —
tools/load,reass,ask WRITE   —       —       —            —       —
tools/types          read    read    read    read         read    —
tools/calc_dose      —       —       —       —            —       —  (unchanged)
lib/response-val     —       WRITE   —       —            —       read
lib/skill-loader     —       WRITE   —       —            —       read
lib/region           —       WRITE   —       —            —       read
lib/tool-call-id     —       WRITE   —       —            —       read
skills/dose-calc/    read    read    WRITE   read         —       —
DESIGN.md, docs/     —       —       —       WRITE        —       read
app/console/chat*    —       —       —       —            WRITE   read
app/console/console  —       —       —       —            —       WRITE
app/api/chat/        —       —       —       —            —       WRITE
README.md            —       —       —       —            —       —    (deferred, per devex-review 2026-05-28)

ZERO same-file collisions across Phase 2 lanes. ✅
Phase 3 modifies only files no Phase 2 lane touched. ✅

CONFLICT FLAGS (potential, mitigations)
────────────────────────────────────────
1. tsconfig.json: written in Phase 1 only. Phase 2 lanes read it. ✅ Safe.
2. package.json: NO lane modifies it. If a lane needs a new dep, STOP and
   tell the user — adds a Phase 2.5 rebase step. ✅ Documented.
3. node_modules: not in git. Each worktree shares a single npm install
   (npm install in trunk; worktrees symlink node_modules). Run
   `npm install` in each worktree first time only. ✅ Documented.
4. .vscode/, .claude/: each session may write its own .claude/ state.
   Gitignored. ✅ No collision.
5. WORKTREE PRUNING: parallel sessions doing `git worktree prune` could
   stomp each other's lock files. ★ DON'T RUN GIT WORKTREE PRUNE during
   Phase 2. ★

ESTIMATED WALL TIMES
────────────────────
Phase 1 (single session, sequential):        ~20min
Phase 2 (5 parallel sessions, max of):       ~60min wall time
  (Lane B: 50min · Lane C: 45min · Lane D: 30min · Lane F: 60min · Lane E-prep: 30min)
Fan-in check:                                 ~5min
Phase 3 (single session, sequential):        ~60min
TOTAL WALL TIME:                             ~2.5hr
TOTAL OPUS COMPUTE:                          ~5-6hr (parallel-multiplied)
```

---

## §6 — DESIGN.md outline (what the deliverable narrates)

The reviewer reads DESIGN.md first. It must hit these beats in order:

1. **Problem + brief verbatim**
2. **3-layer architecture diagram** (ASCII from §2 above)
3. **Safety spine — two layers, honestly framed:**
   - Primary: `calculate_dose` is a TypeScript function called directly in route code, NOT exposed as an SDK tool. The model passes a `dose_rule_id` (a string). The tool looks up the rule and does the math. The model literally cannot author a number.
   - Secondary: the JSON-block validator. Even though the model can't author the number, the dose UI render is sourced from the validator's structured output, not the model's prose. Defence-in-depth: prose could theoretically *mention* a number; the UI only renders from the validated tool result.
   - Two-line rejection paragraph: marker-interpolation rejected (negative-constraint reliability, digit-gate FP surface, streaming incompatibility); regex-sweep rejected (Unicode digits, spelled-out numerals, tool-arg numerics).
4. **How we arrived here:** link to `docs/iteration-trace/v1.md` through `v3.1.md`
5. **Why a skill, not a pipeline:** Tan's same-input-same-output rule; cite the iteration-2-post-cleanup **226/226 PASS across 17 cases** (12 originals + 5 adversarial — prompt injection, age out of band, weight in pounds, conflicting weights, severity asserted vs features) as evidence of skill quality. Also cite the architectural promise mechanically verified: zero clinical numbers, zero specific differential condition names, zero severity descriptions, zero citations anywhere in the skill markdown — every fact a clinician sees comes from the registry via a validated tool result.
6. **Why not RAG (the deliberate retrieval choice):** the brief says *"queries & retrieves the relevant local guideline using basic RAG, agentic AI/MCP approach **or similar**."* We considered RAG and chose typed registry lookup instead. Rationale: (a) for a single-condition corpus, vector search over one document is theatre — it adds a retrieval-error class without adding retrieval value. (b) Recent work — Sen, Kasturi, Lumer, Gulati, Subbiah, *"Is Grep All You Need? How Agent Harnesses Reshape Agentic Search"* (2025) — finds grep/deterministic retrieval beats vector retrieval inside agent harnesses across Chronos, Claude Code, Codex, and Gemini CLI, because the model reasons better over typed, deterministic tool results than over fuzzy similarity hits. (c) The four-tool contract preserves the option to swap in RAG later behind the same `load_guideline` boundary when the corpus grows beyond ~5 guidelines — the tool's typed refusal kinds (`out_of_scope`, `region_unknown`) are the affordance for that growth. The architectural commitment is *retrieval is a tool boundary*, not *retrieval is vector search*. Cite the paper in `research/papers.md` (full citation; one-paragraph summary of relevance).
7. **Dynamism + regionality:** AU + NZ; the same patient gets different doses across regions; verified citations
8. **DCB0129 hazard log** (cross-reference to skill README)
9. **TGA AI-CDS carve-out** (cross-reference to skill README)
10. **Tool contract:** schemas in `tools/types.ts`; portability claim ("any TypeScript runtime implementing this contract can load the skill")
11. **UI design language:** Heidi-grammar 3-column shell (Variant A, locked design-review 2026-05-28). Warm cream `#f6efe6` background, claret `#5b2230` accent, Source Serif 4 titles, Inter body. Cards EMBED inside the assistant message bubble (dose-card and reassessment-card are NOT siblings of `<Message>` — they're children of it). Safety semaphore from DESIGN.md§233 honoured: amber = intentional clinical decision (refusal, capped, completeness, confirm-weight); red = technical failure (validator-blocked, model unreachable). The visual reference lives at `~/.gstack/projects/joshwilks111-max-clinical-care-partner/designs/heidi-chat-right-rail-20260528/variant-A-heidi-grammar.html`.
12. **Accessibility (D17):** v1 ships full a11y: ARIA landmarks, `role="log" aria-live="polite"` thread, per-card semantic `aria-label`s, `:focus-visible` outlines, `prefers-reduced-motion` honoured, contrast AA verified. Clinical-AI reviewer signal.
13. **Architecture decisions log:** the 20 locked decisions from this plan (D1–D20 across eng, design, dx, ceo reviews)
14. **NOT in scope / TODOS**

---

## §7 — Build sequencing for Day-by-Day (if Sonnet sequential)

**Day 1 morning (~2hr):** Lanes A + B + C + D — types locked, validator + tools + lib all green.

**Day 1 afternoon (~2hr):** Lane E (harness) + Lane F (UI components). Both green individually.

**Day 1 evening (~1hr):** Lane G1–G7 (wire + DESIGN.md + README).

**Day 2 morning (~1hr):** Lane G8–G14 (delete + ship). Buffer for any surprise.

**Day 2 afternoon onwards:** Buffer for Loom recording, demo prep, polish.

---

## §8 — NOT in scope (deferred TODOS)

| # | Item | Trigger to revisit |
|---|---|---|
| 1 | US croup region | If a US clinician trials it |
| 2 | Anaphylaxis (any region) | 2nd condition for "registry beyond croup" demo |
| 3 | Age-banded dosing | Drug class that uses it |
| 4 | Multi-skill orchestration | 5+ conditions OR 2nd workflow |
| 5 | Agentic retrieval | Corpus > context window |
| 6 | Live guideline freshness / hash-and-pin | Deployed pilot |
| 7 | CPG-on-FHIR | Hospital deployment |
| 8 | UpToDate / Micromedex integration | Licensing |
| 9 | Per-hospital overrides | 2nd hospital |
| 10 | Voice input | Heidi's actual surface; PromptInput already supports it |
| 11 | Session persistence (chat survives reload) | Next PR |
| 12 | Streaming-safe per-chunk dose-card detection | Median assistant message > 8s in prod |
| 13 | Multi-tab safety | If clinician opens 2 tabs |
| 14 | Server Actions migration (AI SDK 6) | SDK 6 ships an onFinish-equivalent in Server Actions |

---

## §9 — What already exists (KEEP / REUSE)

| Asset | Reuse |
|---|---|
| `tools/calculate_dose.ts` (296 LOC, 30 tests) | UNCHANGED — the deterministic spine |
| `tools/calculate_dose.test.ts` | UNCHANGED |
| `registry/guidelines.ts` (Starship croup data) | EXTEND — add region + AU + reassessment_plans; preserve Starship verbatim |
| `lib/retry.ts` + `lib/router.ts` + `lib/schemas.ts` | KEEP — used by tools |
| `app/console/console.tsx` + `bluey-shell.regression.test.tsx` | KEEP shell; refactor right rail |
| `app/console/rail.tsx` (left rail) | UNCHANGED |
| `components/ai-elements/*` (8 files) | REUSE — compose chat panel |
| `components/ui/*` (shadcn primitives) | REUSE — buttons, dialogs, dropdowns |
| `docs/architecture.md` + `architecture.png` | REWRITE — new 3-layer story |
| Skill at `~/skills/dose-calculator/` | COPY into `skills/dose-calculator/` (D8) |

---

## §10 — Failure modes (per new codepath)

| Codepath | Failure mode | Test | Error handling | User-visible? |
|---|---|---|---|---|
| `/api/chat` streamText | LLM API timeout | `route.test.ts` mocks timeout | retry once via `lib/retry.ts`, then return refusal | "I cannot reach the model right now" |
| validator parse fence | Malformed JSON | `validator.test.ts` | return `blocked: dose_card_malformed` | "Response could not be validated" |
| validator schema | Extra keys (model slips in `dose_mg`) | `validator.test.ts` | `.strict()` rejects | "Response did not match expected shape" |
| validator orphan id | tool_call_id doesn't match any step | `validator.test.ts` | return blocked | "Response referenced a missing tool call" |
| `get_reassessment_plan` | Stale guideline | `tool.test.ts` | `rule_not_verified` refusal | "Cannot recommend follow-up; guideline review overdue" |
| `load_guideline` | Unknown condition | `tool.test.ts` | `out_of_scope` refusal | "This skill currently supports paediatric croup only" |
| Region cookie | Malformed value | `region.test.ts` | default NZ | (silent) |
| originalNote pin | User clears chat mid-session | `chat-panel.test.tsx` | reset pin on "New chat" click | (UI affordance) |

**Critical gap audit:** every failure mode above has a test AND error handling AND a user-visible message. ZERO silent failures. ✅

---

## §11 — Sign-off

When the build is complete:

```sh
npm install
npm test                                    # ~87 green
npx tsc --noEmit                            # 0 errors
npm run build                               # clean
npm run dev                                 # local sanity: paste Jack T. note, verify cards
# (Promptfoo removed 2026-05-28 — vitest now owns the LLM integration cases.)
git add -A
git commit -m "feat(v3.1): thin-harness + fat-skill rewrite — drop pipeline, ship chat surface"
git push origin claude/elegant-robinson-17acc6
gh pr create --base main --title "v3.1: thin harness / fat skill" --body-file PR_BODY.md
```

Then merge → Vercel auto-deploys → smoke `clinical-care-partner.vercel.app` → record Loom → submit.

---

## §12 — Paste-ready prompts (7 prompts: Phase 1, 5 lanes, Phase 3)

**Pre-build sequence (gate before Phase 1):**

1. **Run `/plan-design-review`** ✅ DONE 2026-05-28 (D13–D17 landed).
2. **Run `/plan-devex-review`** ✅ DONE 2026-05-28 (D18–D20 landed).
3. **Run `/plan-ceo-review`** ✅ DONE 2026-05-28 (RAG defense, deploy-before-delete, rebase gate, live e2e smoke landed).
4. **Pre-Phase-1 REBASE GATE (ceo-review 2026-05-28, Codex finding #11):** The trunk worktree is currently 13 commits behind `origin/main` (per the plan header). Before pasting PROMPT 1, the operator (main Claude session) runs in the trunk worktree:
   ```sh
   git fetch origin
   git status   # confirm clean working tree
   git rebase origin/main   # or git merge --ff-only origin/main
   # resolve any conflicts inline; commit; verify HEAD matches origin/main intent
   git rev-parse HEAD       # echo for traceability
   ```
   This MUST complete cleanly before Phase 1. Starting a 5-lane parallel build from a stale base means the fan-in math is meaningless. If conflicts can't be resolved cleanly, STOP and triage before firing PROMPT 1.
5. THEN proceed to Phase 1.

**Build sequence (after the reviews + rebase gate):**

1. In the trunk Claude Code session, paste PROMPT 1 (Phase 1). Wait for sign-off — Opus echoes `BASE_SHA=<sha>` and the LANES_READY paths from `scripts/dx/fan-out.sh`.
2. In the Claude Code desktop app, spawn 5 side-panel sessions, one per LANES_READY worktree path. Paste PROMPTS 2-6 (one per session). Each lane prompt does NOT need BASE_SHA substitution — fan-out.sh has already created the worktrees on the right commit.
3. Wait for all 5 lanes to push (~60min wall). In the trunk session, run `bash scripts/dx/fan-in-check.sh`. It exits 0 only when all 5 lanes are GREEN with PROGRESS.md showing DONE status.
4. In the trunk session, paste PROMPT 7 (Phase 3). Wait for sign-off (~65min, includes deploy-before-delete safety + e2e smoke).
5. PR is up. Smoke the live URL (the chat surface answers the 5 demo cases correctly). Merge → Vercel auto-deploys prod. THEN record Loom.

---

### PROMPT 1 — Phase 1 foundation (single Opus terminal, run first)

Open Claude Code in the trunk worktree:
```sh
cd "C:\Users\joshw\Heidi Interview Assignment\.claude\worktrees\elegant-robinson-17acc6"
claude
```

Paste:

```
Execute Phase 1 of the v3.1 build plan at .claude/plans/v3.1-build-ready.md (§5 → PHASE 1 — FOUNDATION). Do steps P1.1 through P1.11 verbatim:

0. REBASE GATE (ceo-review 2026-05-28, Codex finding #11) — verify the trunk is current with origin/main BEFORE any other work. Run:
     git fetch origin
     git rev-list --count HEAD..origin/main   # MUST print 0
     git rev-list --count origin/main..HEAD   # any number is fine (your branch is ahead)
   If the first count is NOT 0, the operator skipped the pre-Phase-1 rebase. STOP and ask. The 5-lane fan-out is built off this exact SHA; a stale base means lane branches build off a tree that doesn't match production.

1. git fetch origin main; rebase claude/elegant-robinson-17acc6 off origin/main IF the gate above shows behind-by-N>0 and the user has confirmed re-running rebase is OK; resolve conflicts; drop noise checkpoints; keep real commits. (If P1.0 gate already showed 0-behind, this step is a no-op.)
2. npm install.
3. Update tsconfig.json — add "@skills/*": ["./skills/*"] to the paths block.
4. Copy ~/skills/dose-calculator/ → skills/dose-calculator/ — **selectively**. Do NOT use `cp -r ~/skills/dose-calculator/` (the recursive whole-dir copy would pull in `node_modules/`, `bun.lock`, and `package.json` which belong to the skill's standalone dev workspace, not the harness repo). Instead:
   ```sh
   mkdir -p skills/dose-calculator/{references,scripts,evals}
   cp ~/skills/dose-calculator/SKILL.md skills/dose-calculator/
   cp ~/skills/dose-calculator/README.md skills/dose-calculator/
   cp ~/skills/dose-calculator/HARNESS-BRIEF-get_reassessment_plan.md skills/dose-calculator/
   cp ~/skills/dose-calculator/references/*.md skills/dose-calculator/references/
   cp ~/skills/dose-calculator/scripts/*.ts skills/dose-calculator/scripts/
   cp ~/skills/dose-calculator/evals/*.jsonl skills/dose-calculator/evals/
   ```
   Verify after copy: `ls skills/dose-calculator/` should show: SKILL.md, README.md, HARNESS-BRIEF-get_reassessment_plan.md, references/, scripts/, evals/. NO node_modules, NO bun.lock, NO package.json. Then assert post-cleanup state matches D9:
   - `wc -l skills/dose-calculator/evals/cases.jsonl` MUST print 17.
   - `ls skills/dose-calculator/references/` MUST show exactly croup-flowchart.md + refusal-taxonomy.md (safety-canon removed per the 2026-05-28 cleanup).
   - `ls skills/dose-calculator/scripts/` shows validate_dose_card.ts AND lint_skill_output.ts (both are kept — lint_skill_output.ts is a dev tool that lints skill outputs during iteration; harmless to ship and useful for Lane D's contract test if anything drifts).
   - Grep skills/dose-calculator/SKILL.md for clinical numbers: `grep -E '\b[0-9]+(\.[0-9]+)?\s*(mg|ml|mcg|kg)\b' skills/dose-calculator/SKILL.md` MUST return 0 matches (the D9 architectural promise: clinical content lives in the registry, not the skill markdown).
   - Same grep on `skills/dose-calculator/references/*.md` MUST return 0 matches.
   If any of these assertions fail, STOP and report — the upstream skill drifted from the iteration-2-post-cleanup state the plan was built against.
5. Author tools/types.ts — re-export the Zod schemas from @skills/dose-calculator/scripts/validate_dose_card AND define the RefusalKind unions (CalculateDoseRefusalKind = 4 values from main; LoadGuidelineRefusalKind = "out_of_scope" | "region_unknown"; GetReassessmentPlanRefusalKind = 6 values per spec; SkillDirectRefusalKind = "unresolved_dangers"). Export them all.
6. Verify tools/types.ts compiles: npx tsc --noEmit.
7. Create scripts/dx/fan-out.sh and scripts/dx/fan-in-check.sh per §14 (devex-review 2026-05-28). chmod +x scripts/dx/*.sh. These are the operator-side orchestration scripts the main session uses to fan-out worktrees and verify fan-in. Copy the body of each script verbatim from §14.
8. git add -A && git commit -m "feat(v3.1 phase 1): foundation — tsconfig + skill + type lock + orchestration scripts"
9. git push origin claude/elegant-robinson-17acc6
10. Capture BASE_SHA: BASE_SHA=$(git rev-parse HEAD). Echo it.
11. Run: bash scripts/dx/fan-out.sh "$BASE_SHA". This creates all 5 lane worktrees, runs npm install in each, and prints LANES_READY with the 5 paths. The human will then spawn 5 Claude Code side-panel sessions (one per worktree path) and paste PROMPTS 2-6.

Honour decisions D1-D20. If anything is unclear, STOP and ask. Do not invent fields. Estimated time: ~22min (P1.1-P1.6 ~18min, P1.7-P1.11 ~4min including fan-out script run).

When done, output the BASE_SHA line and the 5 LANES_READY worktree paths clearly so the human can spawn the side-panel sessions.
```

---

### PROMPT 2 — Lane B (terminal 2, after Phase 1 prints BASE_SHA)

Setup (spawn a Claude Code side-panel session — `scripts/dx/fan-out.sh` already created the worktree + ran npm install per D18):
```sh
# Working directory for this side-panel session:
# C:\Users\joshw\Heidi Interview Assignment\.claude\worktrees\lane-B-registry-tools
# (the desktop app's side-panel new-session UI takes a folder; point it here)
```

Paste (the prompt itself does NOT need BASE_SHA substitution — fan-out.sh already created the worktree on the right commit):

```
You are Lane B of a 5-lane parallel build of the v3.1 thin-harness plan at .claude/plans/v3.1-build-ready.md. Read §5 → PHASE 2 → TERMINAL 1 — Lane B for your file ownership.

You OWN (exclusive write):
- registry/guidelines.ts (MODIFY: add region "NZ"|"AU" field per guideline; add AU croup guideline with real RCH Melbourne 2020 OR NSW Health 2018 citation — pick whichever is verifiable; add reassessment_plans[] array per guideline matching the spec in the plan §3 D9; **add differential_check[] array per guideline** listing the must-not-miss conditions and their distinguishing features (read `skills/dose-calculator/HARNESS-BRIEF-get_reassessment_plan.md` — the "Companion contract addition" section at the bottom specifies the shape AND lists the four croup differentials with their distinguishing features; hazard_level on all four is "must_not_miss"); **make severity_rows[].description strings clinically precise** — they are now the source of truth for severity matching since the skill markdown no longer carries that content (see the harness brief for the exact Bjornson & Johnson modified CMAJ 2013 grading text to use); remove the anaphylaxis guideline cleanly)
- registry/guidelines.regression.test.ts (NEW: assert Jack T. 14.2kg → 2.13mg still works after the schema migration; assert AU guideline shape; assert anaphylaxis removed; **assert differential_check[] returns 4 must-not-miss items for croup; assert severity_rows[].description strings are non-empty and match the documented Bjornson & Johnson grading per row**)
- tools/load_guideline.ts (NEW ~100 LOC: signature load_guideline(condition, region) → typed result | typed refusal; **the return shape includes differential_check: DifferentialItem[] — the skill reads this in Phase 2 to know which conditions to check for over-overlap**; refusals are out_of_scope | region_unknown; reads from registry)
- tools/load_guideline.test.ts (NEW ~7 tests: NZ croup ok; AU croup ok; asthma → out_of_scope; unknown region → fallback NZ; severity_rows shape; dose_rules shape; **differential_check shape — returns 4 items for croup with hazard_level "must_not_miss" and non-empty distinguishing_features arrays**)
- tools/get_reassessment_plan.ts (NEW ~120 LOC: signature per spec in the plan §3 D9; 6-step selection logic; 6 typed refusal reasons)
- tools/get_reassessment_plan.test.ts (NEW ~7 tests: mild/moderate plan 120min; severe 240min; invalid_severity_label; invalid_guideline_id; rule_not_verified; no_reassessment_required; branches array shape)
- tools/ask_user.ts (NEW ~40 LOC: structured slot tool, kind enum, returns {answer: string})
- tools/ask_user.test.ts (NEW ~3 tests: kind enum, return shape, structured slot emission)

You IMPORT from (read-only):
- tools/types.ts (Zod schemas + RefusalKind unions — already authored in Phase 1)
- tools/calculate_dose.ts (UNCHANGED, math reference)

You READ (for clinical content the registry must carry):
- skills/dose-calculator/HARNESS-BRIEF-get_reassessment_plan.md — sections "Companion contract addition — `differential_check` on `load_guideline`" and "Companion contract addition — `severity_rows[].description` is the source of truth" specify exactly what the registry JSON entries must contain. The four croup differentials (epiglottitis, bacterial tracheitis, foreign body aspiration, anaphylactic airway oedema) and their distinguishing features are listed there verbatim. The four severity row descriptions (mild / moderate / severe / impending_respiratory_failure) are listed there verbatim.

You DO NOT TOUCH:
- Any file in lib/, app/, skills/, evals/ — those are other lanes' responsibility.
- package.json, tsconfig.json — Phase 1 owns those.

Sign-off command:
  npm test -- registry tools  (all green)
  git add -A
  git commit -m "feat(v3.1 lane B): registry extension (region + reassessment_plans + differential_check + precise severity descriptions) + 3 new tools + tests"
  git push origin lane-B-registry-tools

Honour decisions D1-D20. If a contract is unclear, read skills/dose-calculator/SKILL.md, README.md, AND HARNESS-BRIEF-get_reassessment_plan.md — those three together are the contract source of truth. DO NOT invent fields. Estimated time: ~50min (was 45min; +5min for the differential_check + severity descriptions work).

PROGRESS signal (D19): every ~10 min, append a line to `PROGRESS.md` at the worktree root: `[T+Nm] DONE: <files+tests> · WIP: <current file> · BLOCKERS: <none|description>`. The main session reads these without flipping into your session.

Failure mode (D20): if your sign-off tests fail, you have ONE attempt to triage + fix. If a second test run still fails, STOP. Append to PROGRESS.md: `[T+Nm] STATUS: BLOCKED · <specific error> · attempted: <what>`. Then `git add . && git commit -m "wip: lane-B BLOCKED" && git push origin lane-B-registry-tools` so the main session can pick up. Do NOT loop.
```

---

### PROMPT 3 — Lane C (terminal 3)

```sh
# Working directory for this side-panel session:
# C:\Users\joshw\Heidi Interview Assignment\.claude\worktrees\lane-C-validator-lib
# (fan-out.sh already created the worktree + ran npm install; just spawn the session here)
```

Paste:

```
You are Lane C of a 5-lane parallel build of the v3.1 thin-harness plan at .claude/plans/v3.1-build-ready.md. Read §5 → PHASE 2 → TERMINAL 2 — Lane C for your file ownership.

You OWN (exclusive write):
- lib/response-validator.ts (NEW ~150 LOC: walks event.steps[].toolResults to find tool_call_id matches; parses BOTH ```dose-card and ```reassessment-card fenced JSON blocks; Zod validates via DoseCardEmittedSchema and ReassessmentCardEmittedSchema imported from @skills/dose-calculator/scripts/validate_dose_card; merges tool result fields into the validated card object; returns discriminated union { text, dose_card | null, reassessment_card | null, refusal | null, blocked? })
- lib/response-validator.test.ts (NEW ~14 tests: no fence → null cards; valid dose-card → filled from tool result; valid reassessment-card → filled; malformed JSON → blocked; schema mismatch → blocked; orphan tool_call_id → blocked; card on refused tool result → blocked; both cards present in one message → both render; walks steps[] correctly across a multi-step loop; tool result mutation doesn't leak prose number; whitespace tolerance; fence inside markdown code block; .strict() rejects extra keys; multi-step with load_guideline in step 1 + calculate_dose in step 2)
- lib/skill-loader.ts (NEW ~40 LOC: in dev mode reads skills/dose-calculator/SKILL.md fresh via fs.readFile on every call; in prod mode returns a cached string baked at first call; export getSystemPrompt() function)
- lib/skill-loader.test.ts (NEW ~3 tests: dev fs.readFile; prod caches; missing file errors clearly)
- lib/region.ts (NEW ~50 LOC: getRegion(cookies) → "NZ" | "AU"; default "NZ"; export setRegion helper; cookie name "care-partner-region")
- lib/region.test.ts (NEW ~3 tests: NZ default; AU explicit; malformed cookie defaults NZ)
- lib/tool-call-id.ts (NEW ~20 LOC: nanoid-based generator conforming to ^[a-zA-Z0-9_-]{8,32}$ regex; export newToolCallId() function)
- lib/tool-call-id.test.ts (NEW ~3 tests: regex conformance; 10k uniqueness; length stable at 12)

You IMPORT from (read-only):
- @skills/dose-calculator/scripts/validate_dose_card (Zod schemas — single source of truth)
- tools/types.ts (RefusalKind unions)
- ai (SDK 6 types — OnFinishEvent, StepResult)
- zod
- nanoid

You DO NOT TOUCH:
- Any file in registry/, tools/, app/, skills/, evals/ — other lanes' responsibility.

Sign-off command:
  npm test -- lib  (all green)
  git add -A
  git commit -m "feat(v3.1 lane C): response validator + skill loader + region + tool-call-id"
  git push origin lane-C-validator-lib

Honour D1-D20. The validator MUST walk event.steps[].toolResults (D2 lock) — not destructure event.toolCalls/toolResults directly. The shape is in node_modules/ai/dist/index.d.ts under OnFinishEvent. Estimated time: ~45min.

PROGRESS signal (D19): every ~10 min, append a line to `PROGRESS.md` at the worktree root: `[T+Nm] DONE: <files+tests> · WIP: <current file> · BLOCKERS: <none|description>`.

Failure mode (D20): if your sign-off tests fail, you have ONE attempt to triage + fix. If a second test run still fails, STOP. Append to PROGRESS.md: `[T+Nm] STATUS: BLOCKED · <specific error> · attempted: <what>`. Then commit + push so the main session can pick up. Do NOT loop.
```

---

### PROMPT 4 — Lane D (terminal 4)

```sh
# Working directory for this side-panel session:
# C:\Users\joshw\Heidi Interview Assignment\.claude\worktrees\lane-D-skill-sync
# (fan-out.sh already created the worktree + ran npm install; just spawn the session here)
```

Paste:

```
You are Lane D of a 5-lane parallel build of the v3.1 thin-harness plan at .claude/plans/v3.1-build-ready.md. Read §5 → PHASE 2 → TERMINAL 3 — Lane D for your file ownership.

Your job is the skill-side contract: refresh the committed skill workspace from ~/skills/dose-calculator/ (in case iteration-2 has landed updates), AND write the harness-side contract test that asserts the skill's eval cases.jsonl is consistent with the harness's Zod schemas.

You OWN (exclusive write):
- skills/dose-calculator/SKILL.md (REFRESH if ~/skills/dose-calculator/SKILL.md is newer; otherwise leave as Phase 1 committed)
- skills/dose-calculator/README.md (REFRESH)
- skills/dose-calculator/references/croup-flowchart.md (REFRESH — workflow shape only; should be ~40 lines; the file must NOT contain clinical numbers, drug names with features, or specific differential condition names — those moved to the registry)
- skills/dose-calculator/references/refusal-taxonomy.md (REFRESH)
- skills/dose-calculator/scripts/lint_skill_output.ts (REFRESH — invariant-5 regex guard)
- skills/dose-calculator/scripts/validate_dose_card.ts (REFRESH — single source of truth for Zod schemas; exports DoseCardEmittedSchema, ReassessmentCardEmittedSchema, CalculateDoseRefusalKind, ReassessmentRefusalKind, AnyRefusalKind, nonEmpty helper)
- skills/dose-calculator/HARNESS-BRIEF-get_reassessment_plan.md (REFRESH — the spec Lane B reads)
- skills/dose-calculator/evals/cases.jsonl (REFRESH — should be 17 cases at iteration-2-post-cleanup: 12 originals + 5 adversarial)
- skills/dose-calculator/contract.test.ts (NEW ~17 tests: import cases.jsonl; for each case, assert DoseCardEmittedSchema.safeParse(case.expected_output_shape.dose_card_fields).success === true when emits_dose_card=true; for case-11 longitudinal-reassessment, assert ReassessmentCardEmittedSchema also passes; for case-13 prompt-injection through case-17 severity-asserted, assert the expected refusal_kind is a member of AnyRefusalKind; canary against skill↔harness drift)

You IMPORT from (read-only):
- @skills/dose-calculator/scripts/validate_dose_card (your own committed schemas)

Workflow:
1. Diff ~/skills/dose-calculator/ vs skills/dose-calculator/. If ~/skills is newer, cp -r the differences in.
2. Author contract.test.ts using vitest.
3. npm test -- skills/dose-calculator (all green)
4. git add -A
5. git commit -m "feat(v3.1 lane D): refresh skill workspace + harness↔skill contract test"
6. git push origin lane-D-skill-sync

You DO NOT TOUCH:
- Any file outside skills/dose-calculator/.

Honour D1-D20. The contract test is the canary that catches schema drift when the parallel skill-iteration session bumps SKILL.md — failing fast in CI is the point. Estimated time: ~30min.

PROGRESS signal (D19): every ~10 min, append a line to `PROGRESS.md` at the worktree root: `[T+Nm] DONE: <files+tests> · WIP: <current file> · BLOCKERS: <none|description>`.

Failure mode (D20): if your sign-off tests fail, you have ONE attempt to triage + fix. If a second test run still fails, STOP. Append to PROGRESS.md with `STATUS: BLOCKED`. Commit + push. Do NOT loop.
```

---

### PROMPT 5 — Lane F (terminal 5)

```sh
# Working directory for this side-panel session:
# C:\Users\joshw\Heidi Interview Assignment\.claude\worktrees\lane-F-ui
# (fan-out.sh already created the worktree + ran npm install; just spawn the session here)
```

Paste:

```
You are Lane F of a 5-lane parallel build of the v3.1 thin-harness plan at .claude/plans/v3.1-build-ready.md. Read §5 → PHASE 2 → TERMINAL 4 — Lane F for your file ownership, AND §2 ChatPanel block AND §13 (design-review findings) for visual + behavioural contracts.

Your job is the Heidi-grammar 3-column console — the WHOLE console, not just the right rail. D4 (locked design-review 2026-05-28) migrates today's 2-column `md:grid-cols-[300px_1fr]` shell to a 3-column shell: 220 / 700 / 520. You build all three columns; Phase 3 wires them in app/console/console.tsx (you do NOT modify console.tsx yourself).

VISUAL REFERENCE (open this before you start, then keep it open in a browser tab):
  ~/.gstack/projects/joshwilks111-max-clinical-care-partner/designs/
    heidi-chat-right-rail-20260528/
      ├─ variant-A-heidi-grammar.html  ← the approved layout, pixel-level
      ├─ heidi-screenshot-spec.md       ← what we lifted from the Heidi screenshot
      └─ approved.json                  ← the locked widths + constraints
This is a hand-built reference, not a Figma — match its grammar (colours, spacing, typography, card nesting), not its pixel exactness.

You OWN (exclusive write, all NEW files):

CHAT PANEL — the right rail (520px)
- app/console/chat-panel.tsx (~220 LOC: compose AI Elements — Message, PromptInput, Shimmer, Reasoning, Sources, InlineCitation. Header: "Care Partner" chip + "+ New chat" link (claret #5b2230). Empty state: "Paste a note in the centre, then ask Care Partner." + suggested-prompt trinity (D15) — 3 outline buttons via ButtonGroup: "What dose?" / "What should I watch for?" / "Differential?". Clicking a chip PRE-FILLS the composer (does NOT auto-submit). Thread is role="log" aria-live="polite". Composer has a context chip showing originalNote summary (server-derived per D13). Footer: legalese line + <Select> region toggle. State: messages array, isStreaming bool. "+ New chat" fires confirm if thread has ≥1 message, then DELETEs session-id cookie + clears state per D16. Wire to POST /api/chat with streaming response.)
- app/console/dose-card.tsx (~100 LOC: renders dose_card prop. EMBEDDED INSIDE the assistant message bubble, NOT as a sibling. Layout: header row (uppercase "Dose · oral dexamethasone" + small <Badge> "DETERMINISTIC TOOL" in mono); dose value at 22–24px serif as the VISUAL HEADLINE — "2.13 mg" + route in muted sans alongside; mono trace block beneath ("14.2 kg × 0.15 mg/kg = 2.13 mg · cap 12 mg · capped:false") on a cream-2 background; src-row with InlineCitation. capped:true → amber "CAPPED" chip in the header + "binding limit 12 mg" sub-line. Fields: drug, route, severity_row (from emitted card) + dose_mg, dose_ml, max_mg, capped, source_version, source_url (from validated tool result, merged in by lib/response-validator).)
- app/console/reassessment-card.tsx (~120 LOC: renders reassessment_card prop. EMBEDDED INSIDE the assistant message bubble. Layout: header row with a clock badge (26×26 circle, cream-2 bg, clock glyph) + serif title "Reassess at HH:MM (in Nh)" + muted sub "Three watch-for signs · two branches"; watch-row of amber <Badge> chips (one per watch_for[] item); 2-column grid of <Button variant="outline"> branches (one per next_branches[].if_severity_at_reassessment) — "If worse → escalate" gets the amber-bordered variant; universal_rails footer (dashed top border, muted text). When Phase 5 returned no_reassessment_required, do NOT render this card — render a single small muted line inside the bubble: "No structured reassessment for this drug." When rule_not_verified, render a small amber chip below the dose-card with the refusal kind verbatim + next-action. Fields: watch_for_summary, next_steps_summary (emitted) + reassess_in_minutes, watch_for[], next_branches[], universal_rails[], source_version, source_url (from validated tool result).)
- app/console/refusal-card.tsx (~80 LOC: <Alert variant="safety"> (amber-bordered, NEVER red — red is for technical failure only per D14). Header: <RefusalKind> verbatim in mono small-caps (e.g. "UNRESOLVED_DANGERS"). Body: one-line plain-English gloss. Footer: next-action chip. Fields: kind, message, next_action.)
- app/console/region-toggle.tsx (~50 LOC: <Select> from shadcn with NZ / AU options. Reads/writes via lib/region.ts cookie helper. Switching fires a confirm dialog: "Switch region? This clears the current chat." On confirm: change region cookie + DELETE session-id + reload. Default NZ.)
- app/console/ask-user-form.tsx (~80 LOC: NEW component. Renders ask_user tool prompt inline inside the assistant bubble (per Pass 2 lock). Uses <Alert variant="safety"> (amber-bordered, same semaphore as confirm-weight). Layout: question text in body; <Input> or <Select> per `kind` (weight = number input + "kg" suffix; condition = text input; severity = select with mild/moderate/severe); Submit button. On submit, the answer is POSTed back to /api/chat as the next user turn. NEVER as a modal — modals break the chat flow and read as "something is wrong" not "I need one more thing".)

LEFT RAIL — sessions (220px)
- app/console/session-rail.tsx (~120 LOC: <aside role="complementary" aria-label="Sessions">. Brand block with <H> logo (claret bg, serif "H", 28×28). "+ New session" claret primary button. Nav buttons (Scribe / Evidence / Tasks) via <Button variant="ghost"> with Lucide icons. Grouped session list: "Today" + "Yesterday" headers; sessions render as <Button variant="ghost"> with name + timestamp. Active session: cream-2 bg + 2px claret left-border. v1 ships with the 5 demo cases as stubbed Today/Yesterday sessions per D15: Jack T NZ croup / Jack T AU croup / Mia R ?epiglottitis / Weightless transcript / Asthma 5yo out_of_scope. Clicking a session loads that case's note into the centre via a callback. NO backend — the demo data is static.)

CENTRE COLUMN — the note pane (700px, replaces today's stepped Turn 1/2 stack)
- app/console/note-pane.tsx (~150 LOC: <main role="main" aria-label="Patient note">. Case header (claret-on-cream avatar circle 32×32 with patient initials + serif name + muted sub-line "Paediatric · presenting [date]"). Meta strip: date · timer · "Region: NZ" muted. Action bar: <Button "+ Create plan"> claret + <Button "Resume"> ghost. Tabs (shadcn-style): Note (active) / Transcript / Templates — single styling, only the Note tab is wired in v1; the others render placeholder copy. <Textarea> or rendered <div whitespace-pre-line> for the note — utilitarian, "big enough to paste into". Collapsed <Accordion> beneath the note: "Extracted facts (condition · severity · age · weight)" — this is today's CasePanel facts table, demoted to secondary information.)

TESTS
- app/console/chat-panel.test.tsx (~7 tests: renders empty state with suggested-prompt trinity; chip click pre-fills composer (does NOT submit); fires onSubmit on send; shows Shimmer during stream; renders DoseCard when present (assert it's INSIDE the assistant bubble, not a sibling); renders RefusalCard on refusal; "+ New chat" with messages fires confirm dialog)
- app/console/region-toggle.test.tsx (~3 tests: dropdown reads cookie; onChange writes cookie + DELETEs session-id; confirm dialog fires on switch)
- app/console/dose-card.test.tsx (~4 tests: renders dose value as serif headline; capped:true shows amber CAPPED chip + binding-limit line; missing optional fields don't crash; aria-label includes the dose value)
- app/console/reassessment-card.test.tsx (~5 tests: renders watch chips from watch_for[]; renders 2 branch buttons from next_branches[]; "If worse" branch is amber-bordered; universal_rails renders in footer; no_reassessment_required renders single muted line instead of card)
- app/console/refusal-card.test.tsx (~3 tests: renders each RefusalKind verbatim in mono small-caps; next_action chip present; alert variant="safety" (amber NOT red))
- app/console/ask-user-form.test.tsx (~4 tests: weight kind → number input with kg suffix; condition kind → text input; severity kind → select; submit POSTs answer)
- app/console/session-rail.test.tsx (~3 tests: 5 demo sessions render; clicking a session fires onLoadCase with the right note; active state styles correctly)
- app/console/note-pane.test.tsx (~3 tests: pasted note renders; collapsed facts accordion toggles; case-header avatar uses correct initials)

TOTAL: 8 component files + 8 test files = 16 files. ~920 LOC total (~720 src + ~200 tests). +1.5hr over the original Lane F estimate due to D14/D17 (design tokens + full a11y pass) and the centre + left rail growth from "right-rail-only" to "whole console."

DESIGN TOKENS (add to app/globals.css):
  --cream:        #f6efe6;   /* page bg */
  --cream-2:      #ede5d4;   /* hovered/active rail items, badges, banners */
  --claret:       #5b2230;   /* primary brand + user bubble + new-session CTA */
  --claret-ink:   #ffffff;   /* text on claret */
  --serif:        'Charter','Source Serif Pro','Iowan Old Style','Georgia',serif;
Existing tokens (--safety, --accent, --det, --llm, --ink, --muted, --line) survive untouched. NEVER introduce a new amber or red variant — the existing two cover all six clinical states per D14.

TYPOGRAPHY (next/font/google):
  · Inter — body (loaded via next/font, applied to <html>)
  · Source Serif 4 — titles, dose-card headline value, reass-card title, case-header name (loaded via next/font, exposed as --serif fallback before Charter)
  NEVER fall back to system-ui or "serif" generic for titles. The "I gave up on typography" signal is the #1 AI-slop tell.

ANTI-SLOP RULES (must honour, per Pass 4):
  · All cards STACK vertically inside the assistant message bubble. NEVER grid horizontally — a 3-column [DoseCard][ReassCard][RefusalCard] layout is the canonical AI-slop tell.
  · Composer tool icons: Lucide React (Plus, FileText, BookOpen, Mic), NOT emoji.
  · NO box-shadows on cards. Use border + bg-tone differentiation (cards inside the bubble = #fbf8f1 on white bubble).
  · NO `text-align: center` on titles, body, or cards.
  · Card border-radius: 8–10px. Bubble border-radius: 13px with asymmetric corner (4px on the corner closest to the avatar — 13/13/4/13 for user, 13/13/13/4 for assistant).
  · NO purple/violet/indigo. NO gradients on backgrounds. NO decorative blobs/floating circles/wavy SVGs.
  · Emoji is FINE in user prose (clinician might type one); NEVER as button labels or decorative elements.

ACCESSIBILITY (D17, must ship in v1):
  · ARIA landmarks: role="complementary" left, role="main" centre, role="region" right + role="log" aria-live="polite" on the thread.
  · Each <Message> wraps in <article aria-label="Care Partner reply" | "You asked">.
  · DoseCard: aria-label="Computed dose: 2.13 milligrams oral dexamethasone, from Starship 2020".
  · ReassessmentCard: aria-label="Reassess in 2 hours, three watch-for signs, two branch options".
  · InlineCitation: aria-label="Source: <name>".
  · Validator-blocked alerts: aria-live="assertive". Refusal cards: aria-live="polite" (deliberate, not urgent).
  · Focus-visible: 2px claret outline, offset 2px, on ALL interactive elements. NEVER outline:none without a replacement.
  · Reduced motion: @media (prefers-reduced-motion: reduce) disables shimmer animation + thinking-trace dots. Streaming text still streams (that's content, not animation).
  · Contrast: white-on-claret = 11.4:1 (pass). Muted #7a6e62 on cream #f6efe6 = 4.7:1 (pass AA). VERIFY via DevTools or WebAIM before merge.
  · Touch targets: send button 44×44 minimum (was 42 in mockup — bump). Tool icons 32×32 visual, 44×44 tappable on touch viewports.
  · Cmd/Ctrl+Enter submits composer (matches today's app/console/console.tsx:191 pattern). Esc clears composer draft. Cmd/Ctrl+K opens new chat.

You IMPORT from (read-only):
- @/components/ai-elements/* (Message, PromptInput, Shimmer, Reasoning, Sources, InlineCitation — all 8 exist)
- @/components/ui/* (shadcn primitives — alert, badge, button, button-group, card, dialog, dropdown-menu, input, select, separator, textarea, accordion)
- @/lib/region (Lane C's cookie helper — STUB IT IF LANE C HASN'T LANDED YET; document the stub)
- tools/types (RefusalKind unions — Phase 1's TYPE LOCK)
- lucide-react (Plus, FileText, BookOpen, Mic, Clock icons — already in deps)

You DO NOT TOUCH:
- app/console/console.tsx — Phase 3 modifies it.
- app/console/case-panel.tsx — Phase 3 deletes (facts table migrates into note-pane.tsx accordion).
- app/console/turn1-view.tsx, turn2-view.tsx, phase-loader.tsx, provenance-badge.tsx, fixtures.ts — Phase 3 deletes.
- Any file outside app/console/.
- @/lib/region.ts itself — Lane C writes it.

Sign-off command:
  npm test -- app/console  (all green, expect ~32 tests)
  npx tsc --noEmit (clean)
  git add -A
  git commit -m "feat(v3.1 lane F): heidi-grammar 3-column console (chat + cards + note pane + sessions rail)"
  git push origin lane-F-ui

Honour D1-D20. Open the Variant A reference HTML before you start. The chat panel is the centrepiece UI; the rail and note pane support it. Use existing AI Elements as composition base — do NOT rebuild things from scratch. Estimated time: ~90min (was 60min; +30min for a11y, +30min absorbing rail + note pane). If you hit a design ambiguity, STOP and ask — do NOT improvise on visual decisions.

PROGRESS signal (D19): every ~10 min, append a line to `PROGRESS.md` at the worktree root: `[T+Nm] DONE: <components+tests> · WIP: <current component> · BLOCKERS: <none|description>`. Lane F is the longest lane (~90 min); the main session uses this to know whether to wait or triage.

Failure mode (D20): if your sign-off tests fail, you have ONE attempt to triage + fix. If a second test run still fails, STOP. Append to PROGRESS.md with `STATUS: BLOCKED · <error> · attempted: <what>`. Commit + push. Do NOT loop — Lane F's failure modes tend to be tsc errors that compound when patched blindly.
```

---

### PROMPT 6 — Lane E-prep (terminal 6)

```sh
# Working directory for this side-panel session:
# C:\Users\joshw\Heidi Interview Assignment\.claude\worktrees\lane-E-prep
# (fan-out.sh already created the worktree + ran npm install; just spawn the session here)
```

Paste:

```
You are Lane E-prep of a 5-lane parallel build of the v3.1 thin-harness plan at .claude/plans/v3.1-build-ready.md. Read §5 → PHASE 2 → TERMINAL 5 — Lane E-prep for your file ownership.

Your job is the DESIGN.md rewrite and the docs/architecture.md rewrite. Promptfoo was removed (decision 2026-05-28 — single eval surface). NO CODE DEPENDS ON OTHER LANES — you can run in parallel with B, C, D, F.

You OWN (exclusive write, all NEW or REWRITE):
- DESIGN.md (REWRITE per plan §6 outline: 14 beats including the new beat 6 — "Why not RAG (the deliberate retrieval choice)" — citing Sen et al, *Is Grep All You Need? How Agent Harnesses Reshape Agentic Search* (2025) as evidence that grep/typed-lookup beats vector retrieval in agent harnesses. Hit ALL 14 beats in order; the RAG beat is the brief-fit senior signal added by ceo-review 2026-05-28. Honour the brief verbatim, the two-layer safety framing (D7), the architecture decisions log (20 locked), and "NOT in scope".)
- research/papers.md (APPEND or REWRITE: add the Sen et al *Is Grep All You Need? How Agent Harnesses Reshape Agentic Search* (2025) citation with a one-paragraph "why this matters" summary explaining how the paper's experimental finding — grep outperforms vector retrieval across Chronos + Claude Code + Codex + Gemini CLI harnesses on the LongMemEval benchmark — supports the load_guideline tool's typed-registry choice over RAG for our single-condition corpus. **First: verify the citation against arXiv before writing — search arXiv for the exact title and confirm the authors (Sahil Sen, Akhil Kasturi, Elias Lumer, Anmol Gulati, Vamse Kumar Subbiah) and that the paper is current. If the paper cannot be verified, DO NOT cite it — instead substitute the same argument grounded in first principles only (no citation), and PROGRESS.md log this substitution.** Preserve existing entries in research/papers.md if any.)
- docs/architecture.md (REWRITE: replace turn1/turn1.5/turn2 mermaid with 3-layer skill/harness/tools diagram; cite D7 framing)
- docs/iteration-trace/v3.1.md (NEW one-paragraph summary of this review pass plus the four cleanup passes — Promptfoo removed, safety-canon removed, clinical numbers stripped from references, verified clinical content moved to registry via differential_check + severity_rows[].description)

You IMPORT from (read-only):
- @skills/dose-calculator/scripts/validate_dose_card (Zod schemas)
- skills/dose-calculator/evals/cases.jsonl (eval cases)

You DO NOT TOUCH:
- README.md (Phase 3 rewrites it last)
- Any file in app/, lib/, registry/, tools/ — other lanes.
- promptfooconfig.yaml or evals/fixtures/ or evals/assertions/ — those are GONE (Promptfoo removed).

Sign-off command:
  npx markdownlint DESIGN.md docs/architecture.md  (or skip if no lint configured)
  git add -A
  git commit -m "feat(v3.1 lane E-prep): DESIGN.md rewrite + docs/architecture.md rewrite + iteration-trace v3.1"
  git push origin lane-E-prep

Honour D1-D20. The DESIGN.md is the deliverable the reviewer reads first — invest in the prose. The two-layer safety framing (D7) is the senior signal. Estimated time: ~30min (was 45min; -15min after Promptfoo removal).

PROGRESS signal (D19): every ~10 min, append a line to `PROGRESS.md` at the worktree root: `[T+Nm] DONE: <sections written> · WIP: <current section> · BLOCKERS: <none|description>`.

Failure mode (D20): N/A for E-prep (no tests). If you get stuck on prose for >10 min, append `[T+Nm] STUCK: <which beat>` to PROGRESS.md and ask in the main session.
```

---

### PROMPT 7 — Phase 3 fan-in (back in terminal 1, after all 5 lanes push)

Switch back to the trunk terminal:
```sh
cd "C:\Users\joshw\Heidi Interview Assignment\.claude\worktrees\elegant-robinson-17acc6"
git fetch origin
# verify all 5 lanes pushed:
git branch -r | grep -E 'lane-(B|C|D|F|E-prep)'
```

Paste:

```
Execute Phase 3 of the v3.1 build plan at .claude/plans/v3.1-build-ready.md (§5 → PHASE 3 — FAN-IN + HARNESS + SHIP). Do steps P3.1 through P3.17 verbatim.

1. Cherry-pick each lane in dependency order (D, B, C, F, E-prep):
   git fetch origin
   git cherry-pick BASE_SHA..origin/lane-D-skill-sync
   git cherry-pick BASE_SHA..origin/lane-B-registry-tools
   git cherry-pick BASE_SHA..origin/lane-C-validator-lib
   git cherry-pick BASE_SHA..origin/lane-F-ui
   git cherry-pick BASE_SHA..origin/lane-E-prep
   Resolve any merge conflicts inline (should be rare per file-isolation matrix in §5).

2. npm install
3. npm test (all lane tests still green together?). If red: triage cross-lane type mismatch, fix, commit "fix(v3.1 fan-in): <reason>".

4. BUILD THE HARNESS — app/api/chat/route.ts (~220 LOC) per §3:
   - streamText with claude-opus-4-7
   - 4 tools: load_guideline, calculate_dose, get_reassessment_plan, ask_user
   - System prompt from lib/skill-loader.ts (calls getSystemPrompt())
   - tool_call_id from lib/tool-call-id.ts
   - onFinish callback walks event.steps[] per D2 and calls lib/response-validator
   - Pin originalNote on first user turn (look for the first message with role:"user"; subsequent turns get it injected as system context "The patient under discussion: <originalNote>")
   - Return JSON: { text, dose_card | null, reassessment_card | null, refusal | null, blocked? }
   - app/api/chat/route.test.ts (~7 integration tests with mocked LLM):
     * Jack T. NZ happy path → both cards present
     * Jack T. AU happy path → different dose + different reassess window
     * Missing weight → ask_user fires
     * Unresolved dangers (epiglottitis) → skill direct refusal, no calculate_dose call in trace
     * Out of scope (asthma) → load_guideline returns refusal, surfaced verbatim
     * Multi-turn context retention (originalNote pinning across 3 turns)
     * Region toggle changes dose

5. WIRE THE UI — modify app/console/console.tsx:
   - Replace <EvidencePanel /> in the right rail with <ChatPanel originalNote={originalNote} onRegionChange={...} />
   - Add state for originalNote, set on first centre-column note paste
   - Update app/console/bluey-shell.regression.test.tsx assertion (was checking EvidencePanel; now check ChatPanel)

6. MANUAL SMOKE (run npm run dev, hit http://localhost:3000):
   - Paste Jack T. NZ note → verify stream + DoseCard + ReassessmentCard render
   - Paste Jack T. AU note → verify different dose, different reassessment window
   - Paste no-weight note → verify ask_user message → reply with "14 kg" → verify dose
   - Paste epiglottitis note → verify direct refusal card with "unresolved_dangers" verbatim
   - Type "asthma 5yo dose?" → verify "out_of_scope" refusal card

7. LLM INTEGRATION EVAL (vitest):
   npx vitest run app/api/chat/route.test.ts
   All 7 integration cases must pass with mocked tool returns from
   skills/dose-calculator/evals/cases.jsonl.
   (Promptfoo removed 2026-05-28 — single eval surface; the LLM cases
   are vitest integration tests with mocked LLM responses, not separate
   Promptfoo runs.)

REORDERED 2026-05-28 by ceo-review (Section 9: deploy-before-delete safety).
Original order deleted pipeline files BEFORE Vercel preview deploy — a failed preview
would leave the user with no working surface on the branch. New order ships the
preview FIRST while old code is still in tree, smokes it, THEN deletes.

8. PRE-DELETE GATES (all must pass locally):
   npm test (full suite, expect ~87 green)
   npx tsc --noEmit (typecheck clean)
   npm run build (production build clean)

9. PUSH + PREVIEW DEPLOY (old pipeline still in tree):
   git push origin claude/elegant-robinson-17acc6
   Wait for Vercel preview comment on the push.

10. SMOKE PREVIEW (the new chat surface on the real Vercel runtime):
    Two passes — first manual, then scripted (ceo-review 2026-05-28, Codex finding #21):

    10a. MANUAL SMOKE — click through the 5 demo cases in the preview UI:
         - Paste Jack T. NZ note → verify stream + DoseCard + ReassessmentCard render
         - Paste Jack T. AU note → verify different dose, different reassessment window
         - Paste no-weight note → verify ask_user inline form → reply "14 kg" → verify dose
         - Paste epiglottitis note → verify direct refusal card with "unresolved_dangers" verbatim
         - Type "asthma 5yo dose?" → verify "out_of_scope" refusal card

    10b. SCRIPTED E2E SMOKE — hit the LIVE Vercel preview /api/chat endpoint with curl:
         (closes the gap the 226 skill cases + 7 mocked vitest integration tests can't:
         the deployed route's actual LLM + tool-loop + validator + Vercel runtime
         behavior. ~10 LOC of bash; runs against the same preview URL as 10a.)

         PREVIEW_URL="<the URL Vercel commented on the push>"
         # Case 1: Jack T NZ — assert dose-card contains "2.13" and drug "dexamethasone"
         curl -sf "$PREVIEW_URL/api/chat" \
           -H 'content-type: application/json' \
           --cookie "care-partner-region=NZ" \
           -d '{"messages":[{"role":"user","content":"<Jack T NZ note here>"}]}' \
           | jq -e '.dose_card.dose_mg == 2.13 and .dose_card.drug == "dexamethasone"' \
           || { echo "FAIL: Jack T NZ"; exit 1; }
         # Case 2: epiglottitis — assert refusal kind verbatim
         curl -sf "$PREVIEW_URL/api/chat" \
           -H 'content-type: application/json' \
           --cookie "care-partner-region=NZ" \
           -d '{"messages":[{"role":"user","content":"<Mia epiglottitis note>"}]}' \
           | jq -e '.refusal.kind == "unresolved_dangers"' \
           || { echo "FAIL: epiglottitis"; exit 1; }
         # Case 3: asthma — assert out_of_scope
         curl -sf "$PREVIEW_URL/api/chat" \
           -H 'content-type: application/json' \
           --cookie "care-partner-region=NZ" \
           -d '{"messages":[{"role":"user","content":"asthma 5yo dose?"}]}' \
           | jq -e '.refusal.kind == "out_of_scope"' \
           || { echo "FAIL: asthma"; exit 1; }
         echo "LIVE_E2E_SMOKE: 3/3 PASS"

    If ANY case fails on preview (either 10a or 10b): STOP. Fix locally, push again,
    re-smoke. Do NOT proceed to step 11.

11. DELETE PHASE (only after preview smoke is green):
    git rm -r app/api/turn1/ app/api/turn1.5/ app/api/turn2/
    git rm prompts/turn1.ts prompts/turn1.5.ts prompts/turn2.ts
    git rm lib/collapse.ts lib/collapse.test.ts lib/collapse.f016-018.regression.test.ts
    git rm lib/note-discriminator-scan.ts lib/note-discriminator-scan.test.ts lib/note-discriminator-scan.generalisation.test.ts lib/note-discriminator-scan.regression.test.ts
    git rm lib/case-state.ts lib/case-state.test.ts
    git rm lib/completeness.ts lib/completeness.test.ts
    git rm lib/refusal-gate.ts lib/refusal-gate.test.ts
    git rm lib/plan-schema.ts lib/plan-schema.test.ts
    git rm app/console/case-panel.tsx app/console/case-panel.test.tsx
    git rm app/console/turn1-view.tsx app/console/turn1-view.test.tsx
    git rm app/console/turn2-view.tsx app/console/turn2-view.test.tsx
    git rm app/console/phase-loader.tsx
    git rm app/console/provenance-badge.tsx
    git rm app/console/fixtures.ts app/console/fixtures-weight-gate.test.ts
    git rm scripts/draft-turn15-trace.ts scripts/measure-prompt-tokens.ts
    git commit -m "chore(v3.1): delete pipeline routes + dead libs + obsolete console views"

12. PUSH DELETE + SECOND PREVIEW SMOKE:
    git push origin claude/elegant-robinson-17acc6
    Wait for Vercel preview to redeploy on the delete commit.
    Re-run the 5-case smoke from step 10. The new chat surface must still
    work after the delete (catches any accidental cross-file imports).
    If preview is green: continue. If red: `git revert HEAD` and triage.

13. README.md — NO REWRITE (per devex-review 2026-05-28).

14. docs/architecture.png regenerate from docs/architecture.md mermaid (use mmdc if installed, otherwise commit the .md and a placeholder).

15. gh pr create --base main --title "v3.1: thin harness + fat skill (Heidi take-home)" --body-file PR_BODY.md

16. (Optional cleanup) git worktree remove .claude/worktrees/lane-B-registry-tools (and C, D, E-prep, F).
    git branch -D lane-B-registry-tools lane-C-validator-lib lane-D-skill-sync lane-E-prep lane-F-ui
    git push origin --delete lane-B-registry-tools lane-C-validator-lib lane-D-skill-sync lane-E-prep lane-F-ui

Honour D1-D20. If any step fails, STOP and report. Do not skip the preview smokes — those are the ship gates that catch Vercel-runtime regressions tests can't see. Estimated time: ~65min (was 60min; +5min for the second preview smoke after delete).

Before pasting this prompt, run `bash scripts/dx/fan-in-check.sh` (D18). It verifies all 5 lane branches pushed and surfaces any BLOCKED-status lanes from their PROGRESS.md tail. Only proceed to Phase 3 cherry-pick when fan-in-check exits 0.
```

---

### Per-lane sign-off checklist (you, before kicking Phase 3)

| Lane | Branch | Expected commits | Sign-off command |
|---|---|---|---|
| B | `lane-B-registry-tools` | 1-3 | `npm test -- registry tools` green |
| C | `lane-C-validator-lib` | 1-3 | `npm test -- lib` green |
| D | `lane-D-skill-sync` | 1-2 | `npm test -- skills/dose-calculator` green |
| F | `lane-F-ui` | 1-3 | `npm test -- app/console` green |
| E-prep | `lane-E-prep` | 1-2 | `npm run lint` green (or skip if no lint) |

When all 5 show green and pushed, proceed to PROMPT 7 (Phase 3).

---

## §14 — Operator-side orchestration scripts (devex-review 2026-05-28)

Two scripts the main Claude Code session runs to orchestrate the parallel build. Each is ~30 lines of bash. The main session creates them in Phase 1 (P1.6) right before the BASE_SHA echo so they're in the trunk worktree before fan-out begins. They are NOT consumed by any lane — only by the main session.

### `scripts/dx/fan-out.sh`

Creates all 5 lane worktrees off `$BASE_SHA`, runs `npm install` in each, prints `LANES_READY` with the 5 paths. The main session runs this ONCE after Phase 1 finishes, instead of pasting 5 manual `git worktree add` commands with substituted SHAs.

```bash
#!/usr/bin/env bash
# scripts/dx/fan-out.sh
# Usage: bash scripts/dx/fan-out.sh <BASE_SHA>
# Creates 5 lane worktrees + branches off BASE_SHA, runs npm install in each.
# Idempotent: skips worktrees that already exist; logs skip lines.

set -euo pipefail
BASE_SHA="${1:?BASE_SHA required as first arg}"
ROOT="$(git rev-parse --show-toplevel)"
WT_ROOT="$ROOT/.claude/worktrees"
LANES=(B-registry-tools C-validator-lib D-skill-sync F-ui E-prep)

echo "Fan-out: creating 5 lane worktrees off $BASE_SHA"
for LANE in "${LANES[@]}"; do
  WT="$WT_ROOT/lane-$LANE"
  BR="lane-$LANE"
  if [ -d "$WT" ]; then
    echo "  [SKIP] $WT already exists"
    continue
  fi
  echo "  [ADD]  $WT (branch $BR)"
  git worktree add "$WT" -b "$BR" "$BASE_SHA"
  ( cd "$WT" && npm install --silent ) || { echo "  [FAIL] npm install in $WT"; exit 1; }
  # Symlink node_modules to trunk's copy to save disk + time (optional optimisation)
  # Comment out if disk space isn't a concern.
  # rm -rf "$WT/node_modules" && ln -s "$ROOT/node_modules" "$WT/node_modules"
done

echo "LANES_READY"
for LANE in "${LANES[@]}"; do
  echo "  lane-$LANE: $WT_ROOT/lane-$LANE"
done
echo ""
echo "Next: spawn 5 Claude Code side-panel sessions, one per worktree path above."
echo "Paste the matching PROMPT 2-6 from §12 into each."
```

### `scripts/dx/fan-in-check.sh`

Verifies all 5 lanes pushed green AND ran their sign-off tests. Prints per-lane status with the commit SHA + test count. Surfaces missing lanes with a triage block. The main session runs this BEFORE pasting PROMPT 7.

```bash
#!/usr/bin/env bash
# scripts/dx/fan-in-check.sh
# Usage: bash scripts/dx/fan-in-check.sh
# Checks all 5 lane branches: pushed? PROGRESS.md final status? tests green locally?
# Exit code 0 = all 5 ready for cherry-pick; non-zero = at least one lane needs triage.

set -uo pipefail
ROOT="$(git rev-parse --show-toplevel)"
WT_ROOT="$ROOT/.claude/worktrees"
LANES=(B-registry-tools C-validator-lib D-skill-sync F-ui E-prep)
GREEN=$'\e[32m'; RED=$'\e[31m'; YELLOW=$'\e[33m'; RST=$'\e[0m'

git fetch origin --quiet || true
FAIL=0
echo "Fan-in check: verifying 5 lanes pushed green"
echo "─────────────────────────────────────────────"
for LANE in "${LANES[@]}"; do
  WT="$WT_ROOT/lane-$LANE"
  BR="lane-$LANE"
  REMOTE_REF=$(git rev-parse --verify "origin/$BR" 2>/dev/null || echo "")
  PROGRESS="$WT/PROGRESS.md"
  if [ -z "$REMOTE_REF" ]; then
    echo "$RED[MISS]$RST lane-$LANE: no remote branch"
    [ -f "$PROGRESS" ] && echo "        last PROGRESS.md: $(tail -1 "$PROGRESS")" || echo "        no PROGRESS.md"
    FAIL=$((FAIL+1))
    continue
  fi
  LAST_COMMIT=$(git log -1 --format='%h %s' "origin/$BR" 2>/dev/null)
  echo "$GREEN[OK]$RST   lane-$LANE: $LAST_COMMIT"
  [ -f "$PROGRESS" ] && echo "        final PROGRESS.md: $(tail -1 "$PROGRESS")"
done
echo "─────────────────────────────────────────────"
if [ "$FAIL" -gt 0 ]; then
  echo "$RED$FAIL lane(s) not ready.$RST Triage options:"
  echo "  (a) cat .claude/worktrees/lane-X/PROGRESS.md  # see where it stopped"
  echo "  (b) cd .claude/worktrees/lane-X && claude     # resume manually in that session"
  echo "  (c) skip the lane and patch its files inline during Phase 3"
  exit 1
fi
echo "$GREEN[ALL GREEN]$RST 5/5 lanes pushed. Ready for PROMPT 7 (Phase 3)."
```

### Lane prompt failure-mode block (D20)

Already inlined at the bottom of each Lane B/C/D/F/E-prep prompt below in §12. Reference copy:

> Failure mode (D20): if your sign-off tests fail, you have ONE attempt to triage + fix. If a second test run still fails, STOP. Append to PROGRESS.md: `[T+Nm] STATUS: BLOCKED · <specific error> · attempted: <what>`. Then commit + push so the main session can pick up. Do NOT loop.

### Lane prompt PROGRESS.md requirement (D19)

Already inlined at the bottom of each Lane B/C/D/F/E-prep prompt below in §12. Reference copy:

> PROGRESS signal (D19): every ~10 minutes, append a line to `PROGRESS.md` at the worktree root: `[T+Nm] DONE: <files+tests> · WIP: <current file> · BLOCKERS: <none|description>`. Do not overwrite earlier lines — append. The main session reads these to monitor without context-switching into your session.

---

## §13 — Design-review findings (2026-05-28)

The full 7-pass design review ran with the user's Heidi-product screenshot as the visual anchor. Five new decisions landed in §1 (D13–D17). The Lane F prompt grew to cover the whole 3-column console, not just the right rail. Per-pass scoring:

| Pass | Before | After | Resolution |
|---|---|---|---|
| 1 — Information architecture | 3/10 | 8/10 | Locked 3-column shell (220/700/520), within-message hierarchy table (dose value at 22-24px serif = visual headline), CasePanel's facts table demoted to collapsed accordion inside the new note-pane. D4 rewritten. |
| 2 — Interaction state coverage | 2/10 | 9/10 | Full state table (loading/empty/error/success/partial) added — empty-state suggested-prompt trinity (D15), validator-blocked variants, `no_reassessment_required` rendered as a muted line not a card, ask_user as inline form per Pass-2 lock. |
| 3 — User journey & emotional arc | 4/10 | 8/10 | 12-step storyboard + time-horizon design (5-sec / 5-min / 5-year). Trust-signals table named the 7 signals Lane F must add (4 were missing): inline cite pills, sources chip, thinking-trace, dose-card micro-badge, composer context chip, amber-vs-red semaphore, confirm-weight inline form. |
| 4 — AI slop risk | 6/10 | 9/10 | Classifier = APP UI. All 7 hard-rejections pass. 11 anti-slop blacklist items each have a "Lane F risk" annotation in the prompt. Typography lock (Source Serif 4 + Inter via next/font; NEVER system-ui). Emoji-as-icons forbidden — Lucide React replaces them. |
| 5 — Design system alignment | 5/10 | 9/10 | Component-to-design-token mapping table (15 UI regions mapped to specific shadcn / ai-elements files). 4 new design tokens added per D14. Safety semaphore from DESIGN.md§233 restated explicitly. |
| 6 — Responsive & accessibility | 1/10 | 9/10 | Responsive spec for 4 viewports (mobile deferred). Full a11y pass (D17) — ARIA landmarks, aria-live thread, per-card semantic labels, focus-visible, prefers-reduced-motion, contrast AA verified. |
| 7 — Unresolved design decisions | n/a | resolved | 3 decisions surfaced and locked: (a) left-rail content = 5 demo cases as stubbed sessions (D15); (b) `+ New chat` = hard reset (D16); (c) `originalNote` pin = server-derived (D13). |

**Overall design score: 4/10 → 9/10.**

**Visual reference artefacts (locked Variant A):**
- `~/.gstack/projects/joshwilks111-max-clinical-care-partner/designs/heidi-chat-right-rail-20260528/variant-A-heidi-grammar.html` — the approved layout
- `…/heidi-screenshot-spec.md` — what was lifted from the user's screenshot
- `…/approved.json` — locked widths + constraints
- `…/variant-B-2col-keep-casepanel.html`, `…/variant-C-chat-only-fullbleed.html` — rejected variants kept for context

**Cross-references applied to the plan:**
- D4 rewritten (was misdescribing today's UI as a "Bluey 3-column shell"; that shell doesn't exist).
- §2 ChatPanel block expanded from 18 lines to ~100 lines of locked grammar.
- §3 file deltas: +6 new files (session-rail, note-pane, ask-user-form, session-store, globals.css addition, layout.tsx mod), -5 deleted files (case-panel + tests, turn1/2-view + tests, phase-loader, provenance-badge, fixtures + test).
- §6 DESIGN.md outline grew two beats (10 = UI design language; 11 = a11y).
- §12 PROMPT 5 (Lane F) rewritten from ~30 lines to ~110 lines with full visual contract, anti-slop rules, a11y requirements, typography lock.

**Strategic flag (out of scope but logged for /plan-ceo-review):** Prior learning `heidi-takehome-finish-not-build` (cross-model 9/10 confidence, 2026-05-26) argued for finishing work (Loom + UI + voice-edit + framing) over a rewrite. The v3.1 plan deletes ~4,000 LOC and builds 24 new files. User confirmed v3.1 is the scope decision (D0). Surfacing here for CEO review traceability — design review did not relitigate.

**Decisions deferred:**
- Mobile responsive (<1024px) — deferred to TODO. v1 ships desktop-only. Justification: Heidi itself is desktop-first.
- Live a11y verification with axe-core / Lighthouse — D17 specifies the rules; runtime verification is a Phase 3 manual gate, not automated.

---

## §15 — Devex-review findings (2026-05-28)

Full POLISH-mode DX review against the operator persona (you tomorrow running the parallel build in the Claude Code desktop app). Three new decisions landed in §1 (D18–D20), two new orchestration scripts shipped to §14, and every lane prompt gained a PROGRESS.md + Failure-mode block. Per-pass scoring:

| Pass | Score | Resolution |
|---|---|---|
| 1 — Getting Started (operator) | 7/10 | Phase 1 + fan-out = ~22 min, can't realistically collapse further. Acceptable. |
| 1 — Getting Started (Heidi reviewer) | 9/10 | README "Try in 60 seconds" + live URL stays. README rewrite REMOVED from v3.1 scope (was misdirected in §3); operator handles in a separate end-of-build doc-update pass. |
| 2 — API/CLI/SDK (lane prompts) | 8/10 | Consistent prompt grammar across 5 lanes. Skill contract (D10) is clean. No friction worth surfacing. |
| 3 — Error Messages & Debugging | 4/10 → 9/10 | Gap 3-A (no fix-loop budget) → D20: "ONE attempt to triage, then STOP and write BLOCKED to PROGRESS.md." Gap 3-B (no triage playbook) → `scripts/dx/fan-in-check.sh` prints a 3-option triage block when lanes are missing. |
| 4 — Documentation & Learning | 9/10 | DESIGN.md outline (§6) at 13 beats after design-review additions. Skill workspace docs locked + committed per D8/D9. README scope removed from v3.1 (will be handled in end-of-build pass). |
| 5 — Upgrade & Migration | N/A | One-shot pipeline → chat migration handled in PROMPT 7 DELETE PHASE. No v1→v2 upgrade story for a take-home. |
| 6 — Developer Environment | 9/10 | Windows paths consistent throughout. Desktop-app side-panel model handles window layout (no terminal juggle). |
| 7 — Community & Ecosystem | N/A | Private take-home repo; one reviewer. |
| 8 — DX Measurement | 9/10 | Sign-off checklist (§11) + Vercel preview smoke + 17 eval cases give operator + reviewer clear measurement points. |

**Overall operator-DX: 6/10 → 9/10.** TTHW: 22 min (Phase 1 + fan-out), Competitive tier.

**Friction points found and resolved:**

| ID | Friction | Fix | Decision |
|---|---|---|---|
| P-3A | 5× manual `<BASE_SHA>` substitution in lane setup blocks | `scripts/dx/fan-out.sh` takes BASE_SHA once; creates all 5 worktrees | D18 |
| P-3B | Zero in-lane progress signal; stuck lanes invisible for 30+ min | Per-lane `PROGRESS.md` append-only, every ~10 min; main session `cat`s all 5 | D19 |
| P-3C | 6 terminal windows juggle | N/A — operator uses Claude Code desktop-app side-panel sessions instead | — |
| P-4A | Fan-in check listed commits but didn't verify pushes | `scripts/dx/fan-in-check.sh` checks remote refs + tails PROGRESS.md; surfaces missing-push triage block | D18 |
| P-5A | No programmatic gate before Phase 3 cherry-pick | fan-in-check.sh exit code; Lane E-prep prompt amended to require it pre-Phase-3 | D18 |
| 3-A | Lane test failures had no wall-clock budget; risk of 30-min loops | D20 caps at ONE retry, then BLOCKED status to PROGRESS.md | D20 |
| 3-B | No triage playbook when a lane fails | fan-in-check.sh prints 3 triage options on failure | D18 |
| 4-A | README.md scope was conflated with DESIGN.md (§3 said "Rewrite per §6") | README removed from v3.1 scope; operator handles in end-of-build doc-update pass | — |

**Cross-references applied to the plan:**
- D12 rewritten (was "5 terminals", now "5 desktop-app side-panel sessions").
- D18-D20 added to §1.
- §5 PHASE 1 steps grew from P1.8 to P1.9 (added script creation; renumbered).
- §5 fan-out instructions rewritten (was "Open 5 new terminals", now "spawn 5 side-panel sessions per LANES_READY paths").
- §12 PROMPT 1 grew from 9 steps to 11 (script creation + fan-out.sh run).
- §12 lane setup blocks (B/C/D/F/E-prep) simplified — no more manual `git worktree add` (handled by fan-out.sh).
- §12 lane prompts (B/C/D/F/E-prep) each gained PROGRESS signal + Failure mode blocks.
- §12 PROMPT 7 amended to require fan-in-check.sh exit code 0 before cherry-pick.
- §14 NEW — orchestration scripts inline (~70 lines bash).
- §3 README row rewritten to "NO REWRITE in v3.1 scope".
- §5 PROMPT 7 P3.9 amended to "NO REWRITE in this phase".
- §5 file-isolation matrix: README.md row changed from WRITE to deferred.

**Cumulative wall-time estimate (updated):** Phase 1 ~22 min (was 20; +2 min for fan-out.sh authoring + run). Phase 2 ~90 min (unchanged; Lane F bottleneck). Phase 3 ~60 min (unchanged). Total ~3hr (was 2.5hr).

**Decisions deferred:**
- Live progress TUI dashboard — `PROGRESS.md` files give enough signal for a 3hr build; a polished TUI is YAGNI.
- Codex outside voice on this DX review — POLISH mode + 4 prior reviews → diminishing returns.

---

## §16 — CEO-review findings (2026-05-28)

HOLD SCOPE mode against the Heidi take-home brief. Plan re-validated against deliverables 1-3. Three new actionable changes landed (the RAG-defense beat in DESIGN.md §6 beat 6, the deploy-before-delete reorder in PROMPT 7 / §5 P3.*, the pre-Phase-1 rebase gate + live e2e smoke). One outside-voice pass via Codex GPT-5.5 produced 25 findings — 3 high-leverage tensions surfaced to the user; the user resolved each individually.

**Brief deliverable mapping (re-verified):**

| Brief deliverable | Plan handles via | Status |
|---|---|---|
| 1. Working prototype: unstructured text in | Note pane `<Textarea>` (Lane F note-pane.tsx) + ask_user inline form (Lane F ask-user-form.tsx) | ✅ Covered |
| 1. Working prototype: RAG/MCP/similar retrieval | `load_guideline()` tool — typed registry lookup (NOT RAG). Defended in DESIGN.md §6 beat 6 (new — ceo-review 2026-05-28) citing Sen et al *Is Grep All You Need?* (2025) as evidence that grep beats vector inside agent harnesses for single-condition corpora. | ✅ Defended |
| 1. Working prototype: weight-based dose calc | `tools/calculate_dose.ts` (unchanged, 296 LOC, 30 tests). The deterministic spine. | ✅ Covered |
| 1. Working prototype: grounded management plan | Skill Phase 4 + Phase 5 → dose-card + reassessment-card → embedded in assistant bubble. | ✅ Covered |
| 1. Working prototype: hosted URL OR <10min local setup | Live URL via Vercel preview + prod. README "Try in 60 seconds" stays. | ✅ Covered |
| 2. Architecture diagram (one-page, labels + rationale) | `docs/architecture.md` + `.png` regenerated in PROMPT 7 P3.13. Lane E-prep rewrites. 3-layer story (skill + harness + tools). | ✅ Covered |
| 3. Loom 5-10min with 2+ test cases + design/trade-offs/limitations | OUT OF v3.1 BUILD SCOPE. Recorded AFTER Phase 3 ships. The 5 demo cases in the left rail give the Loom four "different weights" beats out of the box. | ⏳ Deferred to post-build |

**Findings + resolutions:**

| # | Finding | Resolution | Decision |
|---|---|---|---|
| 1 | Brief says "RAG/MCP/similar" but plan uses typed registry lookup. Reviewer could flag this as missing the brief literally. | DESIGN.md §6 grows beat 6 — "Why not RAG (the deliberate retrieval choice)" — citing Sen et al 2025. Lane E-prep verifies arXiv before citing; substitutes first-principles argument if unverifiable. | RAG-defense |
| 2 | The example brief note contains synthetic-looking PHI ("Jack T."). Worth confirming all eval/demo data is synthetic. | User confirmed. No change. | (no decision) |
| 3 | PROMPT 7 P3.8 DELETE PHASE ran BEFORE Vercel preview deploy. If preview build failed, no rollback path on the branch. | Reordered both §5 inline list AND PROMPT 7 numbered list: pre-delete gates → push → preview deploy → smoke preview → THEN delete → push delete → second preview smoke. ~5 min added to Phase 3. | Deploy-before-delete |
| 4 | (Codex #11) Plan was 13 commits behind origin/main. The rebase step lived as P1.1 inside Phase 1 — buried, not a gate. | Hoisted to a pre-Phase-1 REBASE GATE step in §12 pre-build-sequence. PROMPT 1 now starts with a P1.0 gate that verifies `git rev-list --count HEAD..origin/main == 0` before any other work. | Rebase gate |
| 5 | (Codex #21) The 226/226 PASS skill eval doesn't exercise the deployed Next route + AI SDK tool loop + validator + Vercel runtime. The 7-case mocked vitest integration also doesn't. | Added P3.10b LIVE E2E SMOKE — ~10 LOC of bash that curls the live preview URL with 3 cases (Jack T NZ → 2.13 mg dexamethasone; epiglottitis → `unresolved_dangers`; asthma → `out_of_scope`) and asserts via jq. Catches deployed-route regressions before Phase 3 deletes the old pipeline. | Live e2e smoke |

**Codex tensions surfaced but resolved against the plan (the user kept current approach):**
- **Streaming vs validation (#1, #2, #24):** Codex argued streamed prose tokens can show unvalidated clinical text. User chose to keep streaming because (a) the 17 eval cases assert no numeric token in prose, (b) SKILL.md invariant 1 explicitly forbids numbers-in-prose, (c) the dose-card is the canonical render and visible card always wins. Documented for traceability; not a plan change.
- **Overbuilt-for-take-home (#3, #4, #25):** Codex argued for the wrap-up-not-rewrite path (matches prior learning `heidi-takehome-finish-not-build`). User explicitly chose v3.1 via D0. No change.
- **Lane F at 90 min not credible (#9), Phase 3 at 60 min not credible (#10), feasibility of 5 parallel sessions (#7):** Codex flagged the wall-time estimates as optimistic. The user has prior `dispatch-two-beats-in-progress` experience and accepts the risk. D20 (bounded fix-loop) caps per-lane disasters; fan-in-check.sh surfaces blocked lanes early.
- **"Locked decisions posture is dangerous" (#23):** Codex argued 4 reviews can converge on a bad plan. The user invoked CEO review precisely to test this — and locked in 5 changes from it. The "locked" posture is for build-time agents, not for review-time deliberation.

**The story the Loom must tell (per brief deliverable 3 — locked here as input for the post-build Loom session):**

1. **Open with the safety architecture** (~60 sec): "The model never authors a dose number. Watch — paste a weightless note → `weight_missing` refusal. The dose value lives in `tools/calculate_dose.ts`, a TypeScript function. The LLM passes a `dose_rule_id` string; the function does the math. This is the two-layer safety spine."
2. **Demo case 1: Jack T NZ moderate croup** (~60 sec): "Paste the brief's example note. Watch the differential reasoning, the inline citation chip, the dose card with the trace `14.2 kg × 0.15 mg/kg = 2.13 mg`. The Starship guideline citation links to the actual source."
3. **Demo case 2: Jack T AU moderate croup** (~30 sec): "Same patient, different region. The dose may differ; the citation chip changes to RCH Melbourne 2020. Regionality demonstrated."
4. **Design decisions + trade-offs** (~90 sec): "Why a skill + harness, not a pipeline? The skill is portable. Why typed registry lookup, not RAG? Sen et al 2025 — grep beats vector in agent harnesses for single-condition corpora. The contract supports RAG behind the same tool boundary when the corpus grows."
5. **Known limitations** (~60 sec): "Single condition (croup) in v1; the registry is the growth surface. No live guideline freshness check — the `rule_not_verified` refusal kind is the affordance. The 'next-day' beat: differential-collapse loop with active investigation (per `TODOS.md` item 4)."
6. **Architecture diagram walkthrough** (~60 sec): the docs/architecture.png 3-layer view, click through each box.

**Cross-references applied to the plan:**
- §6 DESIGN.md outline grew to 14 beats (new beat 6 — RAG defense with Sen et al 2025 citation).
- §5 inline P3.* list rewrote ordering (delete-before-deploy → deploy-before-delete + e2e smoke + second preview smoke).
- §12 PROMPT 7 numbered list rewrote ordering to match.
- §12 PROMPT 7 P3.10 grew step 10b (LIVE_E2E_SMOKE bash, ~30 LOC).
- §12 pre-build-sequence grew a pre-Phase-1 REBASE GATE step.
- §12 PROMPT 1 grew step 0 (rebase verification before any other work).
- Lane E-prep prompt grew the research/papers.md citation requirement with arXiv verification.

**Decisions deferred:**
- Streaming-vs-validation rearchitecture — user accepted current trade-off with documentation.
- Region default flip (NZ → AU) — Codex flagged Heidi-Melbourne; user keeps NZ default with AU explicit toggle.
- Mobile responsive — already deferred by design-review.

**Cumulative wall-time estimate (updated):** Phase 1 ~22 min (no change). Phase 2 ~90 min (no change). Phase 3 ~70 min (+5min for e2e smoke 10b, +5min for second preview smoke after delete). Total **~3.2hr** (was 3hr).

---

## GSTACK REVIEW REPORT

| Review | Trigger | Why | Runs | Status | Findings |
|---|---|---|---|---|---|
| CEO Review | `/plan-ceo-review` | Scope & strategy | 1 | CLEAR (PLAN) | HOLD SCOPE against brief. Approach B confirmed. 5 changes landed: RAG defense in DESIGN.md §6 beat 6 (Sen et al 2025), deploy-before-delete reorder in PROMPT 7 + §5, pre-Phase-1 rebase gate (Codex #11), live e2e smoke step 10b in PROMPT 7 (Codex #21), Loom story locked in §16. 3 brief deliverables verified covered; Loom deferred to post-build. Codex outside-voice ran (25 findings) — 3 resolved into plan changes, others accepted as current approach with documentation. |
| Codex Review | `/codex review` | Independent 2nd opinion | 1 | issues_found-resolved | Run as ceo-review outside voice via `codex exec` (gpt-5.5, reasoning-effort=high). 25 findings returned; 3 resolved into plan changes (rebase gate, e2e smoke, deploy-before-delete reinforcement); rest surfaced to user and resolved against keeping current approach with traceability in §16. Tokens: 27,185. Recorded as audit-trail input to ceo-review. |
| Eng Review | `/plan-eng-review` | Architecture & tests (required) | 1 | CLEAR (PLAN) | 8 architecture findings + 4 code-quality + 1 perf, all resolved through D1-D12; iteration-2-post-cleanup DEPTH absorbed via D9-D10; multi-session orchestration locked via D11-D12 |
| Design Review | `/plan-design-review` | UI/UX gaps | 1 | CLEAR (PLAN) | 4/10 → 9/10 overall. D4 rewritten (current console misdescribed); 5 new decisions D13-D17 (server-derived pin, design tokens, suggested-prompt trinity, new-chat hard reset, full a11y); §2 ChatPanel block + §3 file deltas + §6 DESIGN outline + §12 PROMPT 5 (Lane F) all updated; Variant A locked at `~/.gstack/projects/.../heidi-chat-right-rail-20260528/variant-A-heidi-grammar.html`. Lane F scope grew from ~60min to ~90min (+30min a11y, +30min absorbing centre+left). |
| DX Review | `/plan-devex-review` | Developer experience gaps | 1 | CLEAR (PLAN) | score: 6/10 → 9/10, TTHW: 22 min (Phase 1 + fan-out), Competitive tier. 3 new decisions D18-D20 (scripted fan-out, per-lane PROGRESS.md, bounded fix-loop); 2 new operator scripts in §14 (`fan-out.sh`, `fan-in-check.sh`); D12 rewritten (5 desktop-app sessions, not terminals); README rewrite removed from v3.1 scope (was misdirected in §3); every lane prompt gained PROGRESS + Failure-mode blocks. 5 friction points (P-3A,B,C, P-4A, P-5A) resolved via D18-D20. |

- **CODEX:** 25 findings (gpt-5.5 high reasoning). 3 resolved into plan changes (rebase gate, e2e smoke, deploy-before-delete reinforcement). Streaming-vs-validation, overbuilt-for-take-home, lane-F-too-large, locked-decisions-dangerous tensions surfaced and consciously kept against current approach with documented rationale in §16.
- **CROSS-MODEL:** Both Claude (4 reviews) and Codex (1 outside-voice) flagged the takehome-finish-not-build tension. User explicitly resolved via D0 ("v3.1 ships, NOT the wrap-up"). Documented in §13 (design-review strategic flag) and §16 (ceo-review accepted-against-current).
- **UNRESOLVED:** 0 (all 20 decisions locked; 5 ceo-review changes applied)
- **VERDICT:** ENG + DESIGN + DX + CEO ALL CLEARED for build sequencing. Phase 1 ready to fire. Updated wall-time estimate: **~3.2hr total** (Phase 1 ~22min + Phase 2 ~90min + Phase 3 ~70min). The build path: (0) pre-Phase-1 REBASE GATE — verify trunk current with origin/main; (1) paste PROMPT 1 → Phase 1 + fan-out.sh produces 5 LANES_READY paths; (2) spawn 5 desktop-app side-panel sessions, one per path; paste PROMPTS 2-6; (3) main session monitors via `cat .claude/worktrees/lane-*/PROGRESS.md`; (4) when all 5 push, run `bash scripts/dx/fan-in-check.sh` (exit 0 required); (5) paste PROMPT 7 → pre-delete gates → push → preview deploy → manual smoke → **live e2e smoke (10b)** → delete → second preview smoke → PR. THEN record Loom per the §16 story.
